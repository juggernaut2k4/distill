# B2B Pivot — Orchestration Plan & Live Status

_Last updated: 2026-07-15 | Source of truth for B2B pivot execution status and approach_

**Related docs:**
- `docs/brainstorm-b2b-platform-pivot.md` — full requirements, Q&A, objective-impact analysis, decisions
- `docs/brainstorm-ai-template-designer.md` — merged in as the spec basis for the Designer (B2B-03 below)
- `CORE_OBJECTIVES.md` — **v3, rewritten 2026-07-17** per Arun's direct restatement of Clio as API-driven AI Voice Learning Infrastructure with an exclusive scope boundary. Superseded v2.0 (B2B-01). See `docs/scope-gap-analysis-2026-07-17.md` for the accompanying gap analysis against everything built so far — 3 real open questions there await Arun's decision before further content-pipeline/glitch/billing work proceeds.

This file has two parts that update at different rates: the **Orchestration Approach** (stable —
only edit if the approach itself changes) and **Live Status** (update the instant any item changes
state, per Arun's standing instruction — do not batch).

---

## How to read this

- **Status vocabulary** (matches `BACKLOG.md` convention): `Not started` | `In progress` |
  `CEO brief done` | `BA spec needed` | `Approved, build ready` | `Blocked` | `Done`
- **IDs**: `B2B-01`..`B2B-05` for the five originally-sequenced Feature Briefs, `B2B-06`+ for
  briefs that surfaced afterward (provisioning, developer portal, testing/metering, ...), `INFRA-*`
  for the Mia Digital LLC migration track. Referenced in commits/PRs the same way
  `TMPL-XX`/`RTV-XX`/`SESS-XX` are elsewhere in this repo.

---

## Orchestration Approach

**Governance:** No code for user-facing features without an approved spec. Chain is
Arun → CEO Agent (Feature Brief) → BA Agent (12-section Requirement Document, Section 11 Open
Questions must be empty) → CEO approval → build. This applies per-brief, not once for the whole
pivot — the pivot is split into 5 sequenced Feature Briefs (below) rather than one giant brief,
so each stays independently reviewable.

**CEO Agent usage — known caveat:** the CEO Agent has previously only acted substantively on Arun's
own direct messages, not on instructions the Orchestrator relays on his behalf. Each brief dispatch
below will be checked for genuine engagement; a thin/rubber-stamped response gets flagged to Arun
immediately rather than treated as "handled."

**Sequencing (dependency graph):**
```
B2B-01 Core Objectives rewrite
   └─▶ B2B-02 Partner API & multi-tenant architecture
          ├─▶ B2B-03 Designer/Configurator
          ├─▶ B2B-04 Billing/metering  (also needs Foundation item F-02 resolved)
          └─▶ B2B-05 Domain/white-label infra

INFRA-* (Mia Digital LLC migration) — parallel, non-blocking, land before production cutover
```

**Due diligence / anti-staleness mechanisms:**
1. Session-level tasks (`TaskCreate`/`TaskUpdate`) mirror the table below in real time during active
   work — this file is the durable, cross-session record; session tasks are the in-the-moment view.
2. This file is updated the instant any item's status changes — not batched to end of session.
3. Checkpoint reported to Arun after each Feature Brief lands, not silently chained through all five.
4. Foundation open questions (F-01, F-02) are hard blockers on the BA gate by design — a spec cannot
   proceed with open questions, so these cannot silently rot.

---

## Live Status

### Foundation (blocking items — must close before dependent briefs proceed)

| ID | Item | Status | Blocks | Detail |
|----|------|--------|--------|--------|
| F-01 | Ledger storage model confirmation | **RESOLVED 2026-07-13 — Option A** | B2B-04 (indirectly, via B2B-02) | Arun's direct decision: "no user details will be saved. the transcription is read and sent to customer through api and customer saves the transcription summary, action items, glitches etc. we only take relevant information like meeting duration etc and update our counter, create logs on what topic, session id, duration so the customers can verify from their end as well... we also save date and any other timestamps or unique id to locate any given time. probably client id as well for each customer." Migration `072_b2b02_usage_events_resolution_a.sql` applied — `usage_events` table (session id, event type, quantity/duration, timestamps, `partner_account_id`, opaque `partner_reference`, traceable to `webhook_dispatch_log`). Topic is reachable via `clio_session_ref` → `partner_sessions.partner_topic_ref`, not duplicated onto every row. Follow-up build dispatched to make this the unconditional code path (was previously feature-flagged pending this decision). Partners need to build their own DB to receive this via API — onboarding documentation not yet written (tracked separately). |
| F-02 | Real COGS numbers (voice-minute cost, per-LLM-call cost) | **DEFERRED 2026-07-13, by decision** | B2B-04 | Arun: "at this point assume with older numbers, create a backlog to research against the new architecture and finalize the numbers. we can do that once the solution is complete." Proceeding with the stale May-2026 numbers (Recall.ai $0.0108/min, Claude Sonnet ~$0.0002/min, infra ~$0.004/min — ElevenLabs's $0.08/min line is dead, no Hume replacement rate yet) as placeholders, not blocking B2B-04. Backlog item created to research and finalize real numbers against the actual new architecture once the full B2B pivot build is complete. |

### Vendor Decisions (voice/meeting-bot stack)

| ID | Item | Status | Notes |
|----|------|--------|-------|
| V-01 | ElevenLabs → Hume EVI (voice provider) | **Done** (committed `d49754c`, `7a0020a`) | Direct, unconditional owner instruction 2026-07-13 — not a toggle, full code removal. Hume is now the sole voice provider, no branching logic anywhere. 7 files deleted (`lib/voice/elevenlabs-adapter.ts`, `lib/elevenlabs-pool.ts`, `lib/voice/relay-handler.ts`, `server.ts`, `tsconfig.server.json`, `lib/tts.ts`, `app/api/tts/route.ts`), 36 modified, `@11labs/client`/`elevenlabs`/`ws`/`@types/ws` deps removed. `tsc --noEmit` clean, `npm run build` clean, 266/267 tests pass (1 pre-existing unrelated failure, confirmed via revert-and-retest). **Relay-mode removal confirmed safe** — Arun confirmed `MEETING_BOT_AUDIO_MODE` is not set in Vercel, so deleting the ElevenLabs-only server-side relay path (`server.ts` etc., no Hume equivalent ever existed) has no production impact. **Two follow-ups still open, not blocking**: (1) `.env.local.example` needs a manual pass by Arun to remove `ELEVENLABS_*` entries — both the agent and Orchestrator hit a permission block trying to edit it; keep `ELEVENLABS_CUSTOM_LLM_SECRET` despite the name, it's actually a generic admin secret, renaming risks breaking prod auth. (2) A **pre-existing latent bug** was surfaced (not caused by this change, deliberately not fixed to avoid altering live session timing as a side effect): the transcript-tracking ref used for silence-detection was only ever updated in the now-deleted ElevenLabs branch, meaning the silence clock may never have gone stale for Hume sessions — worth a separate investigation into whether auto-end-call/silence-detection has been firing correctly in practice. |
| V-02 | Recall.ai → Attendee (meeting-bot provider) | **Phase 1 done (uncommitted). CORRECTED TIMELINE — see below, this was not a fresh evaluation.** | **Correction (2026-07-13, after Phase 1 agent surfaced `docs/brainstorm/ATTENDEE-HUME-ARCHITECTURE-brainstorm.md`): Attendee + Hume was already the live production configuration since 2026-07-04 — 9 days before this pivot conversation started.** Arun set `MEETING_BOT_PROVIDER=attendee` and `NEXT_PUBLIC_VOICE_PROVIDER=hume` in Vercel himself on that date; Recall/ElevenLabs code was already dormant, not deleted, per that doc's own Q6/Q7 answer. Orchestrator's earlier framing of this as "under evaluation, not decided" in this tracker was wrong — should have checked `docs/brainstorm/` before dispatching external vendor research. Vendor research and code-coupling-audit findings below are still factually valid (pricing, platform coverage, the 3 gaps), just mis-labeled as informing a future decision rather than fixing an already-live one.<br><br>**Production data-quality concern, not yet resolved:** because Gaps 2 and 3 (below) existed for those same 9 days, any real session run through Attendee+Hume since 2026-07-04 was likely missing sentiment tracking, deferred-question detection, and — most importantly — quality evaluation / curriculum-gap detection (silent failure, `quality_error` set, no loud break). **Worth a backfill pass once Phase 1 is validated and deployed — not yet scoped or authorized.**<br><br>Vendor research: hosting confirmed (Arun already on Attendee's hosted cloud, no self-hosting), pricing favorable ($0.50/hr → $0.35/hr volume vs. Recall's ~$0.65/hr equivalent), platform coverage matches (Zoom/Meet/Teams). Code-coupling audit (read-only) found: a partial `MeetingBotProvider` abstraction (`createBot`/`deleteBot` only), and 3 real gaps — (1) `inngest/session-meeting-setup.ts` (the production cron) hardcoded Recall.ai directly, bypassing the abstraction entirely; (2) `app/api/attendee/webhook/route.ts` was a feature-incomplete parallel implementation missing sentiment/deferred-question/ice-breaker logic; (3) quality evaluation + RTV-03 accuracy reporting were hardcoded to Recall's transcript format with no Attendee equivalent.<br><br>**Phase 1 (build parity + flip default, keep Recall code as rollback) — DONE, uncommitted:** cron and admin test-session route now use `getMeetingBotProvider()`; dead `createBotRelayMode` deleted; Attendee webhook ported sentiment/deferred-question/ice-breaker logic (duplication chosen over shared-handler refactor — preserves an intentional ElevenLabs-era `pending_transcript` design distinction, new isolated `last_participant_transcript` column added instead); new `fetchAttendeeTranscript()` normalizes Attendee's utterance-level transcript into Recall's word-level shape for quality-eval/RTV-03 (confidence caveat: shape sourced from docs/web search, not an independently verified live API response — recommend a smoke test before trusting in production); `sessions.meeting_bot_provider` column added, written at bot-creation time, used to route evaluation to the right transcript-fetcher (NULL treated as `'recall'`); `MEETING_BOT_PROVIDER` code-level default flipped to `attendee` (brings local/preview in line with prod, which was already `attendee`). Also independently found and fixed (Attendee path only, Recall's file untouched) a latent bug where Recall's ice-breaker subtopic attribution always resolves to `subtopic-0` due to re-querying already-nulled fields. `tsc --noEmit` clean, 266/267 tests pass (1 pre-existing unrelated failure). **Two new migrations not yet applied to Supabase — `069_attendee_ice_breaker_transcript.sql`, `070_sessions_meeting_bot_provider.sql` — code will error at runtime on the new columns until these are applied.** Nothing committed.<br><br>**Phase 2 (delete Recall.ai code entirely) — NOT authorized, pending Arun validating Phase 1 in practice.**<br><br>**Separate, bigger, still-undecided item surfaced by the same brainstorm doc:** a much larger planned architecture change — switching Hume from Custom-LLM (our code as the brain) to Hume's own native LLM with one upfront mega-prompt, self-run sessions, plus two new unbuilt requirements (post-session action-item/glitch extraction via Hume's Chat History API, visualization PDF export). Doc status: "Investigation only. Nothing built. No spec approved yet," explicitly gated behind Arun's standing "spec before build" rule. Phase 1 above did NOT build any of this. Awaiting Arun's explicit answer on whether to pursue it now (→ CEO Agent), hold, or treat as superseded — a bare "ok" was not treated as authorization for this given the stakes. |

### Feature Briefs (CEO → BA → Build pipeline)

| ID | Feature Brief | Status | Blocked By | Notes |
|----|----------------|--------|-------------|-------|
| B2B-01 | Core Objectives rewrite (supersedes `CORE_OBJECTIVES.md`) | **Done** | — | CEO brief → BA spec (`docs/specs/B2B-01-requirement-document.md`, 0 open questions) → CEO approved (verified line-by-line against brief + brainstorm doc §3, no drift). `CORE_OBJECTIVES.md` v2.0 written to repo root 2026-07-13, not yet git-committed. B2C-killed stated as hard premise with an explicit anti-resurrection clause. Data-boundary statement written to hold regardless of F-01's eventual resolution. Objective 3 deliberately left open on which effort owns the 3-level config mechanism (Template Designer thread or a later brief) — by design, not an oversight. |
| B2B-02 | Partner API & multi-tenant architecture | **Done** (built, uncommitted) | B2B-01 (done) | Full chain complete: CEO brief → BA spec (`docs/specs/B2B-02-requirement-document.md`, 0 open questions, produced `architecture.md`) → CEO approved (verified two-auth-system separation and the new `partner_sessions` table reasoning against live code) → migration `071` applied (`partner_accounts`, `partner_admin_users`, `partner_api_keys`, `partner_sessions`, `webhook_dispatch_log`) → built (`lib/partner/*`, `/api/partner/v1/*` routes, `/api/admin/partner-keys*`, HMAC-signed webhook dispatcher with backoff, `/partner-render/[clio_session_ref]` deliberate placeholder stub for B2B-03). 61 new tests + full suite (804 passed, 4 pre-existing unrelated failures). Migration `072` (F-01 Resolution A) deliberately not applied — usage ledger reads go through `webhook_dispatch_log` directly (Resolution B) until F-01 resolves; gated behind `PARTNER_USAGE_LEDGER_ENABLED` (off). One scope call flagged: build agent added `PATCH /api/admin/partner-accounts/:id/outbound-config` (in `architecture.md` but not literally enumerated in the requirement doc) since nothing else could ever populate the outbound credentials every downstream contract depends on. |
| B2B-03 | Designer/Configurator | **Done** (built, committed `a49e151`) | B2B-02 (done) | **Tracker correction 2026-07-13 (during B2B-05 CEO brief research): this row was stale.** Full chain was actually complete: CEO brief (`.claude/agents/clio/feature-briefs/B2B-03-designer-configurator.md`) → BA spec (`docs/specs/B2B-03-requirement-document.md`) → built and committed (`a49e151`, "feat(configurator): B2B-03 Designer/Configurator, partner theming, custom templates") — `lib/partner/{theme,questionnaire,topics-config,content-generation,custom-templates,render-data,live-render}.ts`, `/app/partner-render/[clio_session_ref]`, `/app/partner-questionnaire/[partner_account_id]`, migration adding `partner_questionnaires`/`questionnaire_dispatch_log`/`partner_topic_config`/`partner_content_items`/`partner_theme_config`/`partner_template_config`/`partner_component_config`/`partner_design_preference`/`partner_custom_templates` (architecture.md §12.1). Scope resolved 2026-07-13: Arun confirmed customer-facing (partner-facing) but NOT end-user-facing for the Configurator itself — partners configure it, their own end users never see it. Hard requirement, his own words: "we need to make sure that other partner changes impact all other customers so each partner should be isolated and modular (parameter driven) from their configuration screen" — strict multi-tenant isolation. Replaced the B2B-02 `/partner-render/[clio_session_ref]` placeholder with the real content-pull + theme-resolve + Hume-driving render path (architecture.md §12.6). One item the B2B-03 spec itself explicitly deferred, by name, to B2B-05: subdomain/custom-domain routing for both public render surfaces, and the domain settings-field UI — both remain Clio-hosted paths today, confirmed still true when B2B-05 was scoped (see B2B-05 row). |
| B2B-04 | Billing/metering | **Done** (built, committed `a49f1dc`/`34f7e92`) | B2B-02 (done) | **Tracker correction 2026-07-13: also stale** — was showing "uncommitted, awaiting Arun's go-ahead"; confirmed committed via `git log` during B2B-05 research (`a49f1dc` "feat(billing): B2B-04 wallet/metering...", `34f7e92` "fix(stripe): remove dead B2C-era webhook branches, align lib/stripe.ts to B2B"). Full chain complete: CEO brief → BA spec (`docs/specs/B2B-04-requirement-document.md` v1.1, 0 open questions, produced `architecture.md` §13) → CEO sent back once (2 precision gaps: `days_remaining` null-tie-break rule, `avg_daily_burn_usd` formula) → BA closed both → CEO re-reviewed and approved → migration `075` applied (`partner_wallets`, `billing_rate_versions`, `wallet_ledger`, `usage_events` idempotency columns/index) → built (`lib/partner/webhooks.ts` idempotency fix + `applyWalletDecrement()`, reworked `lib/stripe.ts` for prepaid wallet funding, 3 new `/api/admin/billing/*` routes, new `GET /api/partner/v1/wallet`, `/dashboard/admin/clients` admin page, `lib/billing/{sort,metrics}.ts`). `tsc --noEmit` clean, full test suite passes (1 pre-existing unrelated failure, confirmed via `git stash`). Known deviation, resolved in the follow-up commit: `lib/stripe.ts`'s `getPlanFromPriceId` and all other dead B2C-era webhook branches were removed once confirmed to have zero live callers. |
| B2B-05 | Domain/white-label infra | **Done** (built, uncommitted) | B2B-02 (done), B2B-03 (done) | Full chain complete: CEO brief (`.claude/agents/clio/feature-briefs/B2B-05-domain-whitelabel-infra.md`, verified against live code that this was real unbuilt work, not something B2B-03 solved) → BA spec (`docs/specs/B2B-05-requirement-document.md` v1.2, produced `architecture.md` §14) → CEO caught one real gap on first review (the `embedded`-prop screen-reuse mechanism assumed a single return per component, but `QuestionnaireBuilderClient`/`ContentConfigClient` each have 2 separately-wrapped branches) → BA fixed with per-branch duplication (not a merged check — the two branches pass different `title`/`backHref` props) → CEO re-reviewed and approved → migration `076` applied (`partner_accounts` domain columns + `onboarding_completed_at`, new `partner_onboarding_progress` table) → `@vercel/sdk` added to `CLAUDE.md`'s approved list → built. **Scope**: subdomain-first hosting, custom-domain upgrade via Vercel Domains API, Host-header tenant-resolution middleware (extends, doesn't replace, the existing Clerk gate), new `/dashboard/configurator/domain` screen, plus the full onboarding wizard Arun confirmed 2026-07-13 ("build it now, as part of this brief") — 7 steps (Questionnaire → Topics → Content → Visualization → Domain → Payment method → Go-live), save-and-resume via new `partner_onboarding_progress` table, non-blocking domain verification, reusing B2B-03/B2B-04's existing screens via the `embedded` prop rather than rebuilding them. `tsc --noEmit` clean (independently re-verified), 384/385 tests pass (1 pre-existing unrelated failure, same as B2B-04's). **Real pre-existing bug found and fixed along the way**: `/partner-questionnaire/(.*)` (B2B-03's public questionnaire route) was missing from `middleware.ts`'s `isPublicRoute` list — would have redirected real unauthenticated end users to `/sign-in`, breaking the entire public questionnaire flow; confirmed against the live file and fixed. `.env.local.example` additions (`CLIO_ROOT_DOMAIN`/`VERCEL_API_TOKEN`/`VERCEL_PROJECT_ID`/`VERCEL_TEAM_ID`) skipped — permission-blocked for both agents and Orchestrator, same as the earlier RTV env var item; needs Arun to add manually. Not yet committed — awaiting go-ahead. **This closes all 5 Feature Briefs in the B2B pivot's original sequence (B2B-01 through B2B-05).** `INFRA-07` (final brand/root-domain decision) still "in progress" — the root domain is a single `CLIO_ROOT_DOMAIN` config value throughout, never hardcoded, so this doesn't block. |
| B2B-07 | Developer Portal (docs + playground) | **Fully live 2026-07-16.** Documentation + Playground screens at `/dashboard/configurator/developer` and `.../developer/playground`. `sessions_create` Playground control enabled per Arun's direct confirmation (2026-07-16): a test-mode Playground dispatch should behave like any other test-mode API call — real bot, bounded by B2B-08's existing trial gate, no separate Playground-specific safeguard needed. All 4 endpoints now fully testable. `tsc --noEmit` clean. | B2B-06 (v3 shipped), B2B-08 (trial gate is the intended safety mechanism, confirmed sufficient) | Item 5 ("documentation/developer" screen) and item 6 (JSON playground) of Arun's original 8-point vision, confirmed NOT BUILT by an earlier gap analysis (no OpenAPI spec, no `/docs` route, no playground anywhere in the repo). CEO brief written (`.claude/agents/clio/feature-briefs/B2B-07-developer-portal.md`), grounded in `docs/reference-vendor-api-integrations.md`, `docs/brainstorm-partner-signup-integration.md`, `B2B-06-partner-provisioning.md` v2, `docs/specs/B2B-02-requirement-document.md`, `architecture.md` §3/§7/§10, and the live `app/api/partner/v1/*` route files (verified directly, not from spec text, since routes can drift from spec). **Most significant finding: a same-day contradiction between two source documents on Clio's own partner-auth mechanism.** `docs/brainstorm-partner-signup-integration.md` (2026-07-14/15) records Arun's direct correction — "We need this advanced login now itself. Let's not start with static API" — meaning OAuth2 Client Credentials is the confirmed day-one default. But `B2B-06-partner-provisioning.md` v2 (also revised 2026-07-15) still says the opposite in its own text: "OAuth2 Client Credentials: explicitly named fast-follow, not v1/v2 scope," with the static API key as v2's actual in-scope mechanism. Reading both together: the brainstorm conversation happened but was never written back into B2B-06 v2's document text — v2 is stale on this one point despite being dated the same day. Verified against live code: no OAuth2 token endpoint, `client_id`/`client_secret` issuance, or `/oauth2/token`-shaped partner route exists anywhere (`grep`-confirmed) — only the existing static-key mechanism is live today. CEO did not silently pick one document over the other or guess — resolved by grounding in Arun's most recent, most explicit verbatim instruction (the brainstorm doc), and named the finding explicitly rather than burying it: **B2B-06 needs a v3 revision (or a dedicated follow-on brief) to actually build the OAuth2 client_id/secret + token endpoint mechanism before B2B-07's Playground credential-flow and auth documentation can complete.** Scoped B2B-07 so it isn't fully blocked in the meantime: the Documentation screen's non-auth content (all 4 partner-facing endpoints' schemas, the webhook contract, rate limits) and the Playground's read-only usage/wallet flows can ship independent of B2B-06 v3's timeline; only the sessions-endpoint Playground flow and the OAuth2-specific auth section are phased behind it. Also verified and excluded: domain-config endpoints (`/api/admin/configurator/domain*`) are Clerk-session-gated (`requirePartnerAdmin()`), not part of the partner-API-key-authenticated surface — confirmed via direct code read, correctly kept off the Documentation screen rather than assumed either way. Playground design decision: hits the real API using the partner's own `test`-mode credential (not a full mock) — justified by the existing `mode: 'test'/'live'` distinction already used for billing exclusion (`partner_api_keys.mode`, `partner_sessions.test_mode`), giving partners real validation errors and real response shapes. **One hard pre-condition flagged, not glossed over**: `dispatchMeetingBot()` (`lib/partner/session-init.ts`) does not currently branch on `test_mode` before calling Attendee — a real Google Meet URL pasted into the Playground and sent against the live sessions endpoint would dispatch a real, billable bot today. This is the same gap already named as an open item in the brainstorm doc; B2B-07 does not resolve it but makes it a named, hard pre-condition on shipping the Playground's sessions-endpoint "Send" button. Credential-generation UI is scoped to live only in B2B-06's own Configurator screen — B2B-07 links to it, does not duplicate it. No new npm dependency justified (plain `<textarea>` for the JSON body editor, hand-rolled endpoint cards reusing the existing Configurator design system — a JSON-editor package or an OpenAPI-renderer like Swagger UI would be unjustified for a 4-endpoint surface and would introduce a second visual language). Zero open questions blocking BA dispatch, with the one named phasing dependency the BA should sequence around, not treat as resolved. Next: dispatch BA Agent for the Requirement Document. |
| B2B-08 | Testing/Metering | **Fully shipped 2026-07-16.** `POST /api/admin/billing/test-block` built (mirrors the wallet-topup checkout route), plus a minimal internal-admin trigger button on `PartnerBillingClient.tsx`. `createTestBlockCheckoutSession()` now has a real caller. `tsc --noEmit` clean. | B2B-02 (done), B2B-04 (done, schema/mechanism dependency) — does NOT block on or wait for B2B-06 | CEO brief written (`.claude/agents/clio/feature-briefs/B2B-08-testing-metering.md`), grounded in `docs/brainstorm-partner-signup-integration.md` item 3 + "Resolved during follow-up discussion", `docs/reference-vendor-api-integrations.md` §7/§8, `docs/specs/B2B-04-requirement-document.md`, migration `075`, `lib/partner/session-init.ts`, `app/api/partner/v1/sessions/route.ts`, and `B2B-06-partner-provisioning.md` v2. Confirmed by direct code read (not assumed): `dispatchMeetingBot()` does not branch on `test_mode` today — a `test`-mode key dispatch already sends a real, unbounded, unmetered Attendee+Hume bot at real cost to Clio, forever, with zero cap (cross-confirmed independently by the B2B-07 row below). Arun's "20 min free, then pay per 2-hour block, real bot, cost on us" is therefore the fix that *bounds* an already-live gap, not a mechanism built from nothing. Six items resolved, each with reasoning not just an answer: (1) allowance tracked lifetime-per-`partner_account_id` on new `partner_wallets.trial_minutes_used`, not per-session (a per-session reset would leave the exploitable shape of the existing unbounded gap intact); (2) a real server-side forced cutoff (new `inngest/partner-trial-cutoff.ts`, modeled on the existing `session-timer.ts` precedent but scoped to `partner_sessions`, calling Attendee's existing outbound `leave` endpoint) pulls the bot mid-session at the allowance boundary — checking only at natural session-end was rejected as insufficient to actually bound Clio's cost exposure; (3) the 2-hour block is a real, fixed-price ($1.80) one-time Stripe Checkout charge crediting a **new, dedicated** `partner_wallets.test_minutes_balance` counter, deliberately NOT routed through `balance_usd` — two code-confirmed reasons: `applyWalletDecrement()` already unconditionally skips wallet mutation for `test_mode=true` events (B2B-04 §5.B.1), and conflating test spend into `balance_usd` would corrupt the admin page's real-revenue reporting; (4) rate is $1.80 flat = 120 min × the existing seeded `voice_minute` `$0.0150/min` platform-default rate, same `cogs_placeholder_2026_05_no_margin` label, no new figure invented, per F-02's deferral; (5) new additive `usage_events.is_metered_test_usage` flag added alongside the *unchanged* `test_mode` flag — `test_mode` still means "never billed to the partner, permanently" (unmodified); the new flag is a purely Clio-internal signal that real vendor cost was incurred, resolving the exact tension the dispatch task named ("this DOES cost real money... needs careful design, not just reusing that flag as-is"); (6) confirmed orthogonal to B2B-06's guardrail by activation condition (this brief's gate fires only for `test`-mode keys, B2B-06's fires only for `live`-mode keys) — neither brief blocks on the other's build order; named one deliberate positive interaction, not a dependency: the test-block Checkout session uses `setup_future_usage: "off_session"`, so a partner's first block purchase is likely also the first time they attach a payment method, which incidentally satisfies B2B-06's separate `stripe_default_payment_method_id` check for their future live-mode usage. All schema changes additive (2 new `partner_wallets` columns, 1 new `usage_events` column, 1 new `partner_sessions` column, 1 new `wallet_ledger` entry_type + 1 new nullable column, 2 new RPCs mirroring `credit_wallet_balance`/`decrement_wallet_balance`'s exact pattern); one new route (`POST /api/admin/billing/test-block`); one new Inngest job; three small, named modifications to existing files (`sessions/route.ts`, `live-render.ts`'s `handleSessionEnd()`, the Stripe webhook route's `checkout.session.completed` branch). Explicitly out of scope, named not silently dropped: the separate Attendee inbound-webhook signature bypass (unrelated security fix), a partner-facing Configurator UI for buying a block (flagged as a real follow-on gap for B2B-03 to pick up, not built here without its own spec), and retroactive accounting for historical unmetered test usage (forward-only, matching F-01's own declined-backfill precedent). Zero open questions. Next: dispatch BA Agent for the Requirement Document.
| B2B-06 | Partner provisioning | **Fully shipped, verified twice.** Self-serve signup, OAuth2 Client Credentials (`POST /api/partner/v1/oauth/token`), outbound-config UI, funding guardrail, and the dual-FK schema fix are all live. Independently confirmed clean by two separate verification passes (2026-07-16 shipped-code audit; re-confirmed again by a concurrent reconstruction session). No gaps found. | B2B-02 (done, schema dependency); migrations 076/079 applied | **v3, 2026-07-15 — same-day correction of a v2 document error on the auth mechanism, caught by the sibling B2B-07 CEO Agent.** v2 (below in this same cell's history) flipped Q1 to self-serve per Arun's direct override, but its own "Auth/integration setup" text still said OAuth2 Client Credentials was "explicitly named fast-follow, not v1/v2 scope," with the static API key as the actual in-scope v1 mechanism. That was wrong: `docs/brainstorm-partner-signup-integration.md` Decision #2 (same conversation, same day) records Arun's direct correction — *"We need this advanced login now itself. Let's not start with static API"* — meaning OAuth2 Client Credentials is the v1/day-one default, not a fast-follow. v2's document text was simply never updated to reflect the conversation that had already happened. **v3 corrects this, scoped narrowly to the auth-mechanism section only** (`.claude/agents/clio/feature-briefs/B2B-06-partner-provisioning.md`, now v3) — self-serve signup via Clerk Organizations, the outbound-config UI, the payment-guardrail scoping, and the internal-operator recovery routes are all unchanged from v2. New in v3: self-serve `client_id`/`client_secret` generation (reveal-once, same pattern as the outbound signing secret) as a partner's first credential; a new token endpoint `POST /api/partner/v1/oauth/token`, matching the existing `/api/partner/v1/*` flat-route convention and mirroring the exact shape Clio's own upstream vendor already uses for this grant type (Hume's live `oauth2-cc/token` endpoint, `app/api/hume-token/route.ts:18-25`); `requirePartnerApiKey()` (or its replacement) extended to accept a short-lived OAuth2 token alongside the existing static key, with zero changes to the `sessions`/`usage`/`wallet` route call sites themselves; a new table, `partner_oauth_clients` (not an extension of `partner_api_keys` — `client_id` is a standalone identifier, not a truncated-prefix-of-a-secret like `key_prefix`, so overloading one table would produce a confusing 2x2 state matrix for no reuse benefit). **Decision on the static key, made explicitly rather than left ambiguous**: preserved, not deleted, per the standing "no delete without approval" rule and because `partner_api_keys`/`requirePartnerApiKey()`/every `/api/partner/v1/*` route already depend on it correctly — but demoted to secondary, reachable only via the existing internal-operator route (`POST /api/admin/partner-keys`) for SPOC support/recovery, no longer the self-serve partner's default or first-generated credential. The v2-scoped self-serve static-key-generation Configurator call site is replaced by the OAuth2 client-generation flow instead. Everything else — Q1-Q4, the cost-exposure guardrail (now explicitly confirmed credential-form-agnostic), Identity Model, Known Constraints — carried forward unchanged and re-confirmed, not re-derived. New Q5 added, resolving the OAuth2/static-key scoping explicitly. Zero open questions. Next: dispatch BA Agent for the Requirement Document (now against v3). |
| B2B-09 | Session delivery extraction fix + internal glitch dashboard | **Fully shipped 2026-07-16.** All three missing pieces built: `app/api/partner/render/session-chat-id/route.ts` (the `hume_chat_id` write path — `PartnerRenderClient.tsx`'s existing call now actually lands), `app/api/admin/glitches/route.ts` + `summary/route.ts` (back the already-correct dashboard UI). Extraction pipeline and dashboard are now genuinely end-to-end connected, not just individually correct in isolation. `tsc --noEmit` clean. Real transcript extraction on a live partner session not yet observed (needs a real call to confirm end-to-end, not just code-level connectivity). | B2B-02 (done, schema dependency) | **ID collision, resolved.** First written as `B2B-08`; the sibling testing/metering brief (row above) independently claimed the same ID ~3 seconds earlier, both on a simultaneous fresh read of this file that (correctly, at the time) showed no `B2B-08` anywhere. Renamed to `B2B-09` per this brief's own stated tie-break rule ("whichever was written second renumbers") — `B2B-08` is the testing/metering brief, full stop. CEO brief (`.claude/agents/clio/feature-briefs/B2B-09-session-delivery-glitch-dashboard.md`), grounded in `docs/brainstorm-partner-signup-integration.md` Decisions 5–6, `docs/reference-vendor-api-integrations.md` §6–§8 (the exact `file:line`-cited technical gap analysis this brief executes on), `inngest/hume-action-item-extractor.ts`, migration `073`, `lib/partner/webhooks.ts`, `app/api/webhooks/hume/route.ts`, and `app/dashboard/admin/clients` as the admin-page precedent. Confirmed by direct code read, not the reference doc alone: extraction is fully dead for partner sessions (`partner_sessions` has no `hume_chat_id` column, `session_action_items` is hard-FK'd to `sessions(id)`, and `hume_native_config_archives.session_id` is *also* hard-FK'd to `sessions(id)` — a structural finding the reference doc didn't cover, ruling out any archive-first path for partner sessions); the chat_id needed to fix it is already being received and discarded client-side (`PartnerRenderClient.tsx`'s `onConnect` handler ignores the argument `lib/voice/hume-adapter.ts:154` already passes it). Design direction: reuse the two genuinely shared primitives (`formatTranscriptLines()`, `callClaudeForExtraction()`, both already exported), write a new partner-specific orchestration function against a new `partner_session_insights` table rather than forcing `extractActionItemsForSession()`/`getHumeSessionDetails()`/`session_action_items` to serve two structurally incompatible tables; export the currently-private `fetchAllTranscriptEvents()` helper rather than duplicating Hume's Chat History pagination a third time. New `session.insights_ready` webhook event, one Anthropic call producing action items (full detail), glitches (full detail), and psychology (keywords only, the one exception) per Arun's exact spec. Internal dashboard recommended as a simple sortable/filterable table plus one aggregate summary view (`/dashboard/admin/glitches`, following the `/dashboard/admin/clients` precedent) — not AI-assisted clustering, since no precedent for this kind of internal analytics screen exists yet to build clustering against. **Genuine escalation, not a delay tactic — one open question blocks BA dispatch**: full-detail action items/glitches are session content, and reliable webhook delivery (retry-with-backoff, matching every other event type) requires persisting that payload somewhere until delivered — but `CORE_OBJECTIVES.md`'s "Non-Negotiable Data Boundary" (approved 2 days ago as part of B2B-01) and B2B-02's own `webhook_dispatch_log` design comment (`migrations/071:246-251`, "NEVER logged to this or any table") both restrict exactly this. Three options laid out in the brief (bounded-retention persistence in a new table / fully ephemeral with no retry / persist indefinitely) with Option A (bounded retention) recommended, but this revises the practical meaning of a data-governance statement Arun personally approved this week, for enterprise partners where this promise gets tested by a real security review — not a call the CEO will make unilaterally. Everything else in the brief (extraction mechanism, webhook payload shape, dashboard design) is fully specified and does not change shape under any of the three options. Next: **awaiting Arun's decision on the retention question** before BA dispatch — flagged directly per the CEO Agent's standing escalation instruction, not routed through the orchestrator relay. |
| B2B-10 | Attendee webhook — partner session support | **Built 2026-07-16.** `handleEvent()` in `app/api/attendee/webhook/route.ts` now falls back to a `partner_sessions` lookup when the existing B2C `walkthrough_state` lookup misses; new `handlePartnerSessionEvent()` handles all three Attendee event types. `handleSessionEnd()` (`lib/partner/live-render.ts`) gained one optional 5th `targetStatus` parameter (defaults to `'completed'`, zero impact on the existing call site). 9 new integration tests, all passing. `tsc --noEmit` clean; full suite 411/412 (same 1 pre-existing unrelated failure as every other B2B brief this session). Not yet committed. | B2B-02 (done, schema dependency — no new migration), orthogonal to B2B-08/09 | Arun corrected a framing error before this brief was written: the Attendee webhook is not B2C-specific infrastructure needing a rebuild for B2B — it's Clio's general-purpose Attendee event handler, already proven, that should be *extended*, not replaced. CEO brief (`.claude/agents/clio/feature-briefs/B2B-10-attendee-webhook-partner-sessions.md`) found the correlation mechanism already exists undocumented: `dispatchMeetingBot()` passes `partner_sessions.id` into the Attendee provider's `userId` metadata slot (verified via `lib/partner/session-init.ts` and `lib/meeting-bot/attendee.ts`), so every partner-session webhook event already carries the right correlation key — zero new dispatch-time tagging needed. Per-event-type design: `joined_recording` confirmatory-log-only (bot id already persisted at dispatch); `ended`/`fatal_error` is a fallback safety net (checks `partner_sessions.status` first — no-ops if the client-side `endSessionOnce()` path already completed the session, otherwise fallback-completes via the same `handleSessionEnd()` the client path uses, computing duration from `(now − updated_at)`); `transcript.update` no-op (Data Boundary — never persists partner conversation content, matches B2B-09's own boundary); `participant_events.join_leave` no-op, correlated+logged only (no greeting — explicitly deferred to Arun as a named follow-on, not built speculatively: partner branding/copy is a product decision, and no delivery mechanism to a live Hume session exists yet). **CEO review caught one real factual error** before approval — the same "described-as-wired-but-isn't" bug class found across B2B-08/09: the spec claimed `endSessionOnce()` has three call sites (Hume `end_session`, unmount, adapter disconnect); direct code read showed only two — `onDisconnect` never calls it, only sets UI status. Corrected in the spec (no design change needed; the fallback logic already covers both root causes identically since it only checks `partner_sessions.status`), CEO re-reviewed and approved. Zero schema changes. |
| B2B-11 | Prompt behavior configurability + live join greeting | **Built 2026-07-16, migration 080 applied.** New `partner_prompt_config` table (5 dual-mode JSONB fields, 2 instruction-only TEXT fields), 3 new `partner_sessions` columns (`join_greeting_pending`, `join_greeting_participant_first_name`, `assembled_prompt_snapshot`). `assembleHumeNativePrompt()` extended with a non-overridable partner-guidance block strictly after all 12 fixed rules. New `GET`/`PATCH /api/partner/render/join-greeting/[clio_session_ref]` route. New 2s poll loop in `PartnerRenderClient.tsx`. `tsc --noEmit` clean; full suite 446/447 (same 1 pre-existing unrelated failure). Migration reviewed and applied directly by the Orchestrator after independent re-verification. Not yet committed. **Mandatory pre-ship live smoke test still required before this goes live for a real partner** — confirm mid-session greeting fires correctly and rules 1–12/session content survive intact afterward; not yet performed. | B2B-10 (done, wires the join-greeting flag), B2B-03 (`partner_theme_config` precedent) | Arun asked for two things: (1) partner-configurable session *behaviors* (not content/visualization, which stay separately scoped), dual-mode per field — partner chooses literal text or a free-form instruction Clio interprets; (2) a live-triggered join greeting (explicitly chose Option A over the static-opening-line alternative). CEO analysis inventoried all 12 rules in the Hume prompt template, classifying each as dual-mode-candidate, instruction-only-candidate, fixed, or needs-Arun's-call — Rules 7 (pacing, touches billing) and 9 (AI-disclosure) explicitly deferred, not resolved. Guardrail against prompt injection resolved as structural, not a content filter: partner free-form text lands strictly after the fixed rules in a clearly-labeled non-overridable block — Arun's own pushback correctly reframed the risk from "partner content choices" (legitimate) to "could free text override Clio's fixed platform rules" (the real risk). **CEO review caught one confirmed, serious design flaw before approval — worth naming plainly**: the original design would have sent only the greeting fragment via the existing `sendWrapUpNudge()` mechanism. Verified against Hume's own official documentation (`dev.hume.ai/docs/speech-to-speech-evi/configuration/session-settings`) that a `session_settings.system_prompt` message **fully replaces**, never merges, the EVI session's active prompt — so firing this near session start (not session end, where the existing wrap-up-nudge use case is harmless) would have wiped all 12 fixed rules, tool-calling mechanics, and session content for the rest of any call it fired on, replacing it with just the greeting text. Fixed: a full prompt snapshot (`assembled_prompt_snapshot`) is now persisted at render time and always sent as the prefix of any greeting, with a fail-closed fallback (no send, not a broken send) if the snapshot is ever missing — independently re-verified line-by-line by the Orchestrator, not just trusted from the build report. Five other technical decisions delegated to the BA, all reviewed and approved: new dedicated table (not new columns on `partner_theme_config` — different concern), JSONB `{mode, text}` per dual-mode field (avoids an invalid-state 3-column shape), `TONE_INSTRUCTION_ANCHOR`'s 7,000-char guardrail preserved by appending partner tone text strictly after the anchor sentence rather than editing it, `{firstName}`-style placeholders for partner text (distinct from the engineering-owned `[BRACKET]` convention). Out of scope, named not silently dropped: a partner-admin Configurator UI screen for these fields (data layer + admin API route only, built and callable — the CRUD screen itself is a follow-on). |
| B2B-11 | Prompt behavior configurability + live join greeting | **CEO brief written 2026-07-16, awaiting BA dispatch.** | B2B-03 (done, wiring precedent — `assistant_display_name`), B2B-10 (done, join-greeting no-op this brief closes) | Direct continuation of a prompt-template configurability discussion already had with Arun. Two asks: (1) dual-mode (literal text OR free-form instruction, partner's per-field choice) configurability for a named subset of `HUME_NATIVE_PROMPT_TEMPLATE`'s behavioral rules — deferral phrasing (rule 6), closing question (rule 8b), goodbye line (rule 8c) as dual-mode; verification-question cadence (rule 4) and inter-section recap/bridging style (rule 11) as instruction-only; content/visualization explicitly untouched (already owned by B2B-03's Configurator). Pacing (rule 7, billing-basis risk) and AI-disclosure (rule 9) explicitly named as deferred, not decided, same treatment B2B-10 gave its own deferrals. (2) Arun explicitly chose live-triggered (Option A) join greeting over a static scripted line — reuses B2C's exact proven flag→poll→`sendWrapUpNudge()`→clear pattern (`inngest/session-timer.ts` + `WalkthroughClient.tsx`), wired to B2B-10's already-correlated but currently no-op `participant_events.join_leave` case, landing on a new `PartnerRenderClient.tsx` poll loop (that component has never had one — confirmed directly, a deliberate simplification per its own doc comment that this brief narrowly overrides). CEO Agent independently found and flagged a real technical risk before dispatch: `assembleHumeNativePrompt()`'s existing tone-anchor guardrail (Hume's voice-styling layer only reads the first ~7,000 chars) locates a literal substring that partner tone-instruction customization could silently break — spec must resolve this explicitly, not as an afterthought. Guardrail against prompt-injection via free-form instructions resolved as prompt-structuring (priority language + subordinate block in `assembleHumeNativePrompt()`), not a content-moderation feature — Arun was explicit unusual partner content is a legitimate business choice, the actual risk is instructions trying to override fixed platform rules. Zero open questions blocking BA dispatch; two technical choices (new columns on `partner_theme_config` vs. a new dedicated table; per-field storage shape) left to the BA to decide and document. Next: dispatch BA Agent for the Requirement Document. |
| B2B-12 | Homepage "Get Started" CTA → partner signup | **Built and verified 2026-07-16.** Both buttons (`MarketingNav.tsx`, `app/(marketing)/page.tsx`) now `href="/partner-signup"`. `tsc --noEmit` clean. Not yet committed. | None | Fixes the exact routing bug Arun found live-testing `hello-clio.com`. CEO brief found and fixed BOTH lookalike buttons (nav CTA + bottom banner), correctly excluded 4 other `/onboarding`-linked buttons that don't say "Get started" (hero CTA, 3 pricing-tier CTAs) as out of scope — tracked separately under the broader homepage-redesign backlog item. Zero open questions throughout CEO brief → BA spec → CEO review; approved and built same-session. |
| B2B-13 | Recurring plan tiers (monthly/annual) + configurable top-up amounts | **Fully built and independently verified 2026-07-16.** Migration 081 applied (5 new nullable `partner_wallets` columns, 2 widened CHECK constraints). `lib/billing/plan-tiers.ts` catalog (Starter/Growth, annual = exactly 12× monthly allowance — the bug the CEO review caught and required fixing before approval). Webhook correlation fixed after CEO review found a real bug (API-version-fragile invoice metadata read) — now correlates via `checkout.session.completed` writing `partner_wallets` state, `invoice.paid` looks up by stable `stripe_plan_subscription_id`. Wizard `PaymentStep` redesigned: Plan cards + monthly/annual toggle, upgraded top-up card (presets + free-text). `tsc --noEmit` clean, `npm run build` clean, 475/476 tests. Not yet committed. | B2B-04 (done — wallet/metering pipeline this brief adds a funding mechanism on top of, unmodified) | Closes finding #3's "owed" recommendation below and adds the new "plans (monthly and yearly) and topups" requirement Arun stated directly during the same live-testing pass. Confirmed via direct code read: `lib/stripe.ts` has zero discrete Stripe Price IDs today (all four billing functions use ad-hoc `price_data`), and `partner_wallets` has no recurring-plan-tier concept at all — `tier` is commit-size/support-routing only, `funding_mechanism` is pure pass-through (top-up or auto-recharge-to-a-floor), no bundled-allowance product exists. CEO recommendation, flagged as a product-shape call needing Arun's confirmation rather than assumed: (1) new nullable columns on `partner_wallets` (`plan_tier_key`, `plan_billing_period`, `stripe_plan_subscription_id` — deliberately not reusing `stripe_subscription_id`, which is explicitly scoped "auto-recharge only" by its own existing comment — `plan_current_period_end`, `plan_status`), plan catalog itself as a code constants module (`lib/billing/plan-tiers.ts`) not a DB table, since it's a small rarely-changing list, not something needing `billing_rate_versions`'s historical-integrity treatment; (2) on `invoice.paid` for a plan subscription, credit the wallet by the tier's fixed included-allowance (not the invoice's `amount_paid` the way auto-recharge does) — the one genuinely new mechanic, everything else (negative balance, low-balance alerts) reuses B2B-04 unmodified; (3) wizard `PaymentStep` replaces "Set a monthly minimum" with Plan selection rather than adding a third card (auto-recharge and a bundled-allowance plan are too similar to offer side by side without confusing the choice) — schema/backend for auto-recharge stays, only removed from the self-serve wizard; (4) pricing display recommendation: specific prices in compact cards matching the Configurator's existing plain dark `Card` idiom (not the deleted B2C marketing page's treatment, not a "contact sales" selector) — Arun has real numbers, specificity removes friction, and each card must show price + included allowance together since that comparison is what actually drives a partner's tier choice; (5) top-up amounts get both preset buttons and a free-text field, bounded by the already-existing `min(20).max(50000)` server-side validation, not a new bound. Zero real Stripe Products/Prices created anywhere — four new `PLACEHOLDER_`-prefixed env vars (`STRIPE_PLAN_STARTER_MONTHLY_PRICE_ID` etc., following the pre-pivot CLAUDE.md's own established naming convention) for Arun to fill in once he creates the real Products/Prices himself, exactly as he asked. Explicitly does not touch `usage_events`/`wallet_ledger`/`billing_rate_versions` or any live metering code. 7 open questions left for the BA to close before dispatch to a developer (migration DDL specifics, exact ledger event-source value, full wireframe, final tier names/preset amounts, webhook handler file/diff, one data check on existing auto-recharge subscribers, past-due/canceled lifecycle behavior) — brief explicitly written so BA dispatch is unblocked on the product-shape reasoning, not to skip the zero-open-questions gate. Full brief: `.claude/agents/clio/feature-briefs/B2B-13-recurring-plan-tiers-and-configurable-topups.md`. **ID note**: originally claimed B2B-12, collided with the concurrently-dispatched Get Started CTA fix (which filed first and was already built), renumbered to B2B-13 per the established tie-break rule. |
| B2B-17 | Glitch log → real issue tracker (status/closure/RCA) | **Fully built 2026-07-17, migration 082 applied.** New `glitch_issues`/`glitch_instances`/`glitch_issue_notes` tables, fan-out trigger (capture-only, `OLD.glitches IS NULL` guard), 8 new API routes, 3-panel UI + issue detail view. Purge job built as a sibling Inngest function (`inngest/glitch-instances-purge.ts`) rather than editing the extractor file, to honor the zero-diff invariant — extractor confirmed untouched via `git diff`. Purge-exemption branch (Q1) left as a one-line boolean flip, defaulted FALSE (matches currently-approved 30-day boundary) pending Arun's answer. `tsc`/build/tests clean (475/476, 1 pre-existing unrelated failure). Not yet committed. | B2B-09 (done, extraction pipeline this builds on) | Per Arun's direct instruction today: "we need to build this" (glitch status/closure/RCA gap) and "stays internal with us" (not partner-facing). Adds a status/closure lifecycle + RCA support on top of the existing capture pipeline (`partner_session_insights`, `glitch_summary_by_type_and_partner()`) — extraction/storage/frequency-analytics preserved, not rebuilt. Explicitly does NOT fix the internal-admin RBAC gap (separate, tracked under the Super-Admin backlog item). **Flagged constraint for Arun**: the existing 30-day purge strips glitch descriptions, which would destroy evidence for any issue tracked over weeks — BA must reconcile (exempt attached instances / snapshot before purge / accept aging-out), escalate to Arun if the resolution changes what the purge means. Full brief: `.claude/agents/clio/feature-briefs/B2B-17-glitch-log-to-issue-tracker.md`.
| B2B-20 | Configurator left-nav restructuring (replace linear wizard + unify with post-onboarding Home) | **Fully built 2026-07-17.** New `ConfiguratorSurface.tsx` (unified left-nav+panel, 0 inline styles, responsive lg/md breakpoints, Framer Motion + reduced-motion fallback), `PaymentConfigClient.tsx`/`GoLivePanel.tsx` (extracted from wizard, verified before old code removed), new `configurator-status` aggregator reusing `checkStepComplete()`. `WizardClient.tsx`/`HomeClient.tsx` reduced to redirect stubs, not deleted. `goLive()` rewritten to the Questionnaire+Payment required set. `tsc` clean, lint clean, 497/498 tests (1 pre-existing unrelated failure), `ConfiguratorNavShell` and all section-internal logic confirmed untouched except Integration's mechanical `embedded` prop addition. Two documented deviations, both forced by the "don't touch ConfiguratorNavShell" constraint: container padding reclaimed via negative margins (960px column cap from the shell remains), and post-account-switch section selection falls back to the default-section rule rather than preserving the prior section (the switcher lives inside the untouched shell). Not yet committed/pushed — behavioral/responsive ACs need live UI verification. Full spec: `docs/specs/B2B-20-requirement-document.md`. | B2B-16 (Configurator/API/Docs top-level nav, untouched by this brief), B2B-05 (wizard/`embedded` prop precedent) | Per Arun's direct instruction: "i dont want [Questionnaire/Topics/Content/Visualization/Domain/Payment] like a wizard... have a left pane... group them and categorize them... make it intuitive... very modern and smooth and responsive." CEO brief found the wizard (7 steps, onboarding-only) and Configurator Home (6-card grid, post-onboarding-only) are two separate UI patterns covering an overlapping-but-not-identical 8-area set (Integration is Home-only, Payment/Go-Live are wizard-only) — recommended unifying into one persistent left-nav surface, not locked. BA confirmed unification (the one risk — a new partner needing a forced first-run path — dissolved by a non-gating "Start here" affordance + auto-selected first-incomplete section + completion dots, not a hard gate) and designed the grouping: Learning experience (Questionnaire/Topics/Content/Visualization) / Delivery & integration (Domain/Integration) / Billing (Payment) + pinned Go Live action. Load-bearing technical finding: the whole Configurator is built with inline `style={{}}` objects, which can't express media queries — genuine responsive support is a mechanism change (Tailwind breakpoint classes), not a tweak; independently confirmed by CEO. Completion tracking reuses the existing `checkStepComplete()` function (direct existence checks against config tables) rather than needing new query logic — resolved a problem both the brief and the wizard's own code comments believed was harder than it is. Go Live required set: Questionnaire + Payment only (real, verified completions) — CEO independently confirmed this is *stronger* than the old gate, which accepted "skipped" as complete, allowing a partner to click through all 7 steps with nothing configured and still go live. No new npm dependencies (Framer Motion + Tailwind already sufficient). App-wide responsive audit explicitly kept out of scope — tracked separately in `BACKLOG.md`'s new "🎨 STANDING STORY — Responsive/mobile-friendly by default" perpetual policy (applied incrementally to any screen touched going forward, no upfront sweep), proposed as a future B2B-21 if a dedicated audit is ever wanted. **Deletion-authorization decision (CEO, explicit, not silently passed through)**: Arun's own instruction today already authorizes removing/replacing `WizardClient.tsx`/`HomeClient.tsx` and modifying `lib/partner/wizard.ts`'s `goLive()` — no fresh confirmation required, since every section's functionality is preserved via the existing `embedded` reuse pattern (chrome replaced, no capability lost). Two build guardrails attached: extract-and-verify `PaymentConfigClient`/`GoLivePanel` as working before `WizardClient` is reduced (no commit with net-lost behavior); reduce `WizardClient` to a redirect stub, don't purge it, keeping it recoverable in git. **One courtesy heads-up for Arun, not a gate**: "replace the wizard with a left pane" concretely means the post-go-live card-grid Home is also being absorbed into the unified surface, not just the wizard — flagging in case there's any attachment to that grid, but work proceeds unless Arun objects. Full brief: `.claude/agents/clio/feature-briefs/B2B-20-configurator-left-nav-restructuring.md`.
| B2B-21 | Internal Admin Identity — Super-Admin + Sales-Partner roles, RBAC gate | **Fully built 2026-07-18, migration 084 applied.** `tsc --noEmit` clean (1 pre-existing unrelated error in `HomeClient.tsx`, untouched by this work), `vitest` 497/498 (same 1 pre-existing unrelated failure; templates-PATCH suite's Supabase mock extended for the new lookup, all 20 now pass). **Committed and pushed (`ece1385`) — B2B-22's dev unblocked.** Also folded in a fifth zero-auth route found during the same build, `app/api/admin/billing/invoice/route.ts`, not named in the approved spec's route table — gated it under the same P0 fix; not yet independently CEO-re-reviewed since it wasn't part of the approved spec text. | None (orthogonal to `partner_admin_users`) | Full chain complete: CEO brief → BA spec (`docs/specs/B2B-21-requirement-document.md`, 0 open questions) → CEO reviewed and approved (independently re-verified every claim at source, not trusted from doc text). Per Arun's direct instruction: super-admin accounts (seeded with `hello.arunprakash83@gmail.com`, more addable, all equal peers, act independently) + sales-partner accounts (invite-only by a super-admin, tagged to specific `partner_accounts`) + closing a live authz gap — `/dashboard/admin/*` and its `/api/admin/*` routes today gate on any authenticated Clerk session, not role. New orthogonal layer confirmed correct (`internal_admin_users` + `sales_partner_assignments`, new `requireSuperAdmin()`/`requireInternalAdmin()` helpers) — no touch to `partner_admin_users`/the Clerk-Org webhook path. **Real live security defects found and folded into the same fix, verified directly by both BA and CEO**: `app/api/admin/repair-session-titles/route.ts` and `app/api/admin/seed-topic-cache/route.ts` have **zero auth of any kind** — publicly reachable today, not just under-scoped. A third, previously-undocumented auth path was found and correctly NOT collapsed into the new model: `app/dashboard/admin/templates` runs on `requireSessionAuth`/`lib/session-auth.ts` (a separate Clio-issued JWT), confirmed genuinely global (`template_library` has no `partner_account_id`) — `requireSuperAdmin()` layers on top as an additional check rather than replacing it. `billing/test-block` fixed to also accept `requireSuperAdmin()` (safe — that check already hard-rejects scoped `sales_partner` roles). Invite-token flow reuses `lib/partner/api-keys.ts`'s reveal-once/SHA-256 discipline plus 7-day expiry + single-use + email-match, confirmed stronger than a bare API key. Frozen scope (commission %, e-signature, territory scoping) confirmed absent from all buildable content. Full brief: `.claude/agents/clio/feature-briefs/B2B-21-internal-admin-identity-super-admin-and-sales-partner.md`. |
| B2B-22 | Partner-facing "Known Bugs" screen (per-bug visibility toggle, status+ETA, partner comments/evidence) | **Fully built 2026-07-18, migration 085 applied.** `tsc --noEmit` clean, `npm run build` clean (route manifest confirms `/dashboard/configurator/known-bugs`, `/api/partner/known-bugs*`, `/api/admin/glitches/issues/[id]/partner-visibility*` all compile — this build agent proactively ran the real build, not just `tsc`, learning from the same-day build-failure incident above), 497/498 tests (same 1 pre-existing unrelated failure). Hybrid-scope + reopened-bug edge case both fall out of one shared query (`fetchHybridScopedVisibilityRows`) rather than duplicated logic; read/write split enforced as two separate checks (GET uses hybrid scope, POST comment requires `is_visible = true` directly). One deviation: added `toggled_by_email` to the *internal* visibility-GET response only (not the partner-facing whitelist) so the "last updated by" UI text has something to render — partner-facing response shape left exactly as specified. Committed and pushed alongside the client/server-boundary hotfix. | B2B-21 (done, merged), B2B-17 (internal glitch tracker this reads from) | Full chain complete through CEO approval: CEO brief → BA spec → CEO sent back once (real gap: v1.0's strict `is_visible = true` filter would silently drop a resolved bug's history if an operator ever re-toggled it, undercutting the "past list of issues" trust-building purpose) → BA v1.1 fix (new `first_visible_at` column, hybrid scope: currently-visible OR previously-visible-and-now-closed, never never-visible) → CEO re-reviewed and approved. Per Arun's exact instruction: any super-admin/sales-partner tagged to a partner can toggle a bug's partner-visibility; partner can view + attach evidence/comments but never edit description/status. Real architectural finding: `glitch_issues` has no `partner_account_id`, so visibility is a new per-`(issue_id, partner_account_id)` record (`glitch_issue_partner_visibility`), not a boolean on `glitch_issues` — verified against `supabase/migrations/082_b2b17_glitch_issue_tracker.sql`. Two genuinely distinct read paths (partner-scoped hybrid vs. internal-unscoped-all-bugs), not one shared query with a role filter. BA also specified a read/write split not originally asked for: a sticky-closed-but-hidden bug stays readable but can't receive new partner comments (`can_comment` flag) — CEO confirmed this the right call. Full brief: `.claude/agents/clio/feature-briefs/B2B-22-partner-facing-known-bugs-screen.md`. |
| B2B-23 | Configurator milestone scope reduction, fully-responsive shell, partner content-auth docs | **Fully built 2026-07-18.** `tsc --noEmit` clean, 497/498 tests (1 pre-existing unrelated failure, `voice-gap-watchdog.test.ts`), eslint clean on all touched files. `VISIBLE_SECTIONS`/`StoredWizardStep`/`WizardStep` split, `GO_LIVE_REQUIRED_STEPS = ['integration','payment']`, `SHELL_CONTENT_STYLE` clamp-based fluid shell (replaces both `maxWidth: 960` instances), Integration relabeled to "API base URL," new Docs "Content & image auth" section — all per spec, zero deviations. Two confirmed WS-3 gaps (query-param auth, multi-header auth) logged to `BACKLOG.md`, not built (correctly out of scope). **Committed and pushed (`ece1385`).** | B2B-16, B2B-19, B2B-20 | Full chain complete: CEO brief → BA spec (`docs/specs/B2B-23-requirement-document.md`, 0 open questions) → CEO reviewed and approved (independently re-verified every claim at source — called "one of the more rigorously fact-checked BA specs" reviewed this project). Implements Arun's literal disposition: hide Questionnaire/Topics/Content/Visualization (C1-C4, hidden not deleted), keep Integration + Payment (C6/C7). Kills the hardcoded `maxWidth: 960` dead-space bug in `_shared.tsx` via fluid Tailwind + `clamp()`. **C5/Domain resolved (Arun, Option A)**: white-label Domain section hidden entirely; `outbound_base_url` in Integration relabeled/clarified in place, nothing new built. **Go-Live required set: `['integration', 'payment']`**, nav-dot and Go-Live gate unified onto one `checkStepComplete()` call. BA found and solved two real technical gaps beyond the brief's scope, both CEO-verified against live code: (1) `StoredWizardStep`/`WizardStep` type split needed so `checkStepComplete()` can gain an `'integration'` case without breaking `STEP_COLUMN`/`STEP_ORDER`'s existing 6-key shape; (2) a real regression risk in replacing the hardcoded 960px cap — a CSS-custom-property mechanism keeps `ConfiguratorSurface.tsx`'s existing padding-cancel negative-margin technique correct at every viewport width. C6 (partner-supplied encryption keyword) deferred with reasoning intact. WS-3 content-auth docs spot-checked field-by-field against real Zod schemas/`ssrf.ts`/`crypto.ts` — accurate. **Sequencing note for Orchestrator**: B2B-24's dev work must land after (or in the same merge as) this brief's `GO_LIVE_REQUIRED_STEPS` change, per B2B-24's own CEO review finding. Full brief: `.claude/agents/clio/feature-briefs/B2B-23-configurator-milestone-scope-reduction-responsive-and-content-auth-docs.md`. |
| B2B-24 | Configurator Dashboard/Overview (landing screen, pinned first nav entry) | **Fully built 2026-07-18, composed cleanly on top of B2B-23.** `tsc --noEmit` clean, 497/498 tests (same 1 pre-existing unrelated failure; one flaky run touching B2B-21's in-progress files resolved on rerun, confirmed not caused by this change). New `DashboardPanel.tsx`; `page.tsx` now always defaults to `'dashboard'`; `ConfiguratorSurface.tsx` gained a pinned no-dot `DashboardNavRow`, the old "Start here" hint logic fully removed; `BillingHealth`/`_billing-health.ts` extended with `balance_usd`/`next_billing_date`; `GO_LIVE_REQUIRED_STEPS`/`REQUIRED_LABELS` exported from `wizard.ts`/`GoLivePanel.tsx` and reused (not re-hardcoded) so the Dashboard can never drift from the Go Live panel's actual requirement set/copy. Three deliberate, correctly-reasoned deviations from the literal spec text (all to avoid contradicting B2B-23's actual landed code, not scope creep): imports the real `GO_LIVE_REQUIRED_STEPS` constant instead of a second hardcoded `['integration','payment']` literal; reuses `GoLivePanel.tsx`'s real current requirement copy instead of the spec's stale pre-B2B-23 wireframe text; derives "optional sections" generically as `VISIBLE_SECTIONS minus REQUIRED` instead of hardcoding Domain (which B2B-23 didn't keep visible). **Committed and pushed (`ece1385`).** | B2B-23 (done, composed on top of), B2B-22 (bug data explicitly excluded, owned by that screen instead) | Full chain complete: CEO brief → BA spec (`docs/specs/B2B-24-requirement-document.md`, 0 open questions) → CEO reviewed and approved. Confirms the Dashboard as the Configurator's first pinned nav entry, default landing for every partner state. Four content areas, each reusing an existing data source, no new API routes: (1) setup-completion glimpse via `getConfiguratorStatus()`/`checkStepComplete()`, scoped to `['integration','payment']`; (2) live-status via `partner_accounts.onboarding_completed_at` timestamp ("Live since {date}"); (3) wallet snapshot — extends the existing `getBillingHealth()`/`BillingHealth` shape (`_billing-health.ts`/`_shared.tsx`) to carry `balance_usd`/`next_billing_date` alongside its existing `state`, not a new route; (4) quick-nav tiles with `?section=` deep-links into incomplete sections. Glitch/bug data explicitly excluded — owned entirely by the sibling B2B-22 Known Bugs screen, never duplicated here. **Nav-interaction question resolved as Option A**: Dashboard is the default landing for every partner state (new/mid-setup/live); the nav-level "Start here" hint is removed entirely, its function absorbed by Area 1's own CTA. CEO independently verified this against live `ConfiguratorSurface.tsx` behavior (the hint only ever rendered for not-live partners from any panel) and judged the tradeoff acceptable given B2B-23 shrinks the required set to 2 sections and Dashboard is now one click away — flagged for a live-UI check during `/design-review`, not a spec blocker. Full brief: `.claude/agents/clio/feature-briefs/B2B-24-configurator-dashboard-overview-landing.md`. |
| B2B-25 | Remove Clerk Organizations from partner signup | **Fully built 2026-07-19, committed and pushed (`854f05a`).** `tsc --noEmit` clean, `npm run build` clean (route manifest confirms `/partner-signup/organization` and `/api/webhooks/clerk-organization` are gone; `/partner-signup/[[...partner-signup]]` and `/api/partner-signup/claim` compile), 497/498 tests (same 1 pre-existing unrelated failure). One item left for Arun: remove the now-dead `CLERK_ORGANIZATION_WEBHOOK_SECRET` line from `.env.local.example` — blocked from both the dev agent and the Orchestrator by a sandbox guard on env-pattern files, same as prior RTV/ElevenLabs env-var cleanups this session. Two Clerk-side manual steps also outstanding: delete the `organization.created`/`organizationMembership.created` webhook subscription in the Clerk Dashboard (dead code now, nothing calls it), and clear the 7 test Clerk Organizations + their creator accounts (IDs logged earlier in this doc's changelog) — Supabase-side test data for all 7 was already wiped by the Orchestrator before this brief was written. | None | Per Arun's direct instruction ("i am not very much happy with the organization that clerk provides. too complicated"), followed by a full architecture discussion (`docs/brainstorm-sales-partner-subdomain-routing.md`) concluding Clerk Organizations should be used nowhere in this product — B2B-21 already proved the flat-table alternative works for super-admin/internal-staff; this brief extends the same principle to partner signup, the prerequisite fix before the larger sales-partner/subdomain work proceeds. CEO brief found this is a write-path-only fix: `requirePartnerAdmin()`/`getPartnerAccountsForClerkUser()` already have zero Clerk-Organizations awareness, confirmed by direct code read — only how `partner_admin_users` rows get created changes. No real functionality is lost: no partner-facing "invite a teammate" UI exists anywhere in Clio's own dashboard today, Clerk's hosted org-invite UI was never surfaced in-app. New flow: `/partner-signup` captures a company name directly and attaches it via Clerk `unsafeMetadata`, consumed by a new branch in the `user.created` webhook, reusing the exact `ONBOARD-DATA-01` pattern rather than inventing a new mechanism. `partner_accounts.clerk_org_id` stays nullable/unused rather than dropped; `partner_admin_users`'s shape is unchanged, only its write path. **Real gap found and closed via one CEO revision cycle**: the BA's first pass missed that an already-signed-in visitor reaching `/partner-signup` via the marketing site's "Log in" nav button (not a legacy path — a co-equal top-nav CTA) would hit a silent, permanent dead end under the new flow, since `unsafeMetadata` can't be retroactively attached after `user.created` already fired. Fixed with a new authenticated `POST /api/partner-signup/claim` route and a shared `createOrClaimPartnerAccount()` helper in `lib/partner/signup.ts`, called by both the webhook branch and the new claim route so the two entry points can't drift apart. Single-deploy cutover (not feature-flagged), reasoned through explicitly rather than assumed safe by default. This also permanently closes the bug class fixed earlier this session — the `/partner-signup/organization` catch-all-routing and blank-page-for-signed-out bugs both lived inside Clerk's own `<CreateOrganization>` component's internal navigation, which is being deleted outright. Full brief: `.claude/agents/clio/feature-briefs/B2B-25-remove-clerk-organizations-partner-signup.md`. |
| B2B-26 | Sales-partner entity — signup branch, data model, minimal Clients/Team/Dashboard | **Fully built 2026-07-19, migration 086 applied, committed and pushed (`be6d811`).** `tsc --noEmit` clean, `npm run build` clean (route manifest confirms `/dashboard/channel-partner`, `/dashboard/channel-partner/clients`, `/dashboard/channel-partner/team`, `/team-invite/accept` all present), 500/501 tests (same 1 pre-existing unrelated failure) — includes 4 new test cases directly verifying the `requirePartnerAdmin()` guard clause is a true no-op for real direct partners while correctly rejecting `channel_partner`-kind accounts. Orchestrator independently re-verified the security fix by direct code read before pushing (correct ordering: membership check first, then account_kind check, identical 403 shape, no info leak). | B2B-25 (done, builds directly on its signup flow) | Per Arun's direct instruction: "the only difference between the partner and sales-partner is sales-partner can have multiple clients... lets add a selection, ask the question if you manage multiple clients yes or no. if they say yes then they become a sales-partner. if no then they become a partner." Extends B2B-25's State 1 screen with this Yes/No question; No is today's unchanged flow, Yes creates a new sales-partner entity. **Real naming collision found and resolved**: the literal string `sales_partner` already exists in shipped B2B-21 code (TS union, DB CHECK, `sales_partner_assignments` table, `/api/admin/team/sales-partners` route) for Clio's own internal account-manager staff — a totally different concept. Resolved with collision-free code identifiers (`account_kind='channel_partner'`, `owning_channel_partner_id`, `partner_team_invites`, `requireChannelPartnerAdmin()`) while all user-facing copy still says "sales-partner," confirmed zero exceptions. Data model reuses `partner_accounts`/`partner_admin_users` almost unchanged rather than a parallel entity table — a "client" under a sales-partner is a normal `partner_accounts` row with zero `partner_admin_users` members (the client never logs in, confirmed by Arun earlier). **Real security gap found and closed across two CEO revision cycles**: v1.0 claimed a sales-partner account is "structurally enforced" as a management shell that can never itself act as a direct partner, but the CEO found this was only a missing nav link — a channel-partner admin could type `/dashboard/configurator` directly and fully render it, including issuing a real live `partner_api_keys` row. v1.1 gated the 12 Configurator UI entry points but missed the real scope; the CEO grepped and found `requirePartnerAdmin()` is the sole gate on 42 route files (billing, wizard go-live, content generation, custom domains, OAuth clients, etc.), all still open to direct API calls. v1.2 fixed this at the shared chokepoint — one guard clause inside `requirePartnerAdmin()` itself, provably a no-op for every real direct partner, closing all 42 routes (and any future one) at once rather than 42 manual patches. CEO explicitly authorized overriding its own brief's "don't touch `requirePartnerAdmin`" constraint for this narrow purpose, since that constraint predated discovery of the gap. v1.3 closed a trailing copy-consistency issue (stale "untouched" claims in the scope summary and one acceptance test that would have failed against the actual v1.2 code) via a BA self-review pass, no further CEO cycle needed. Scope deliberately split into 4 briefs (CEO's own call, reasoned explicitly): this brief covers signup + data model + minimal Clients CRUD + Team invite + Dashboard placeholder only. **Named follow-ons, originally sketched as "B2B-27/28/29" — actual numbers superseded 2026-07-19 by Arun's own higher-priority same-day instructions (see B2B-27/B2B-28 rows below, and B2B-30 in the backlog)**: per-client detail (Integration fields, usage cap, routing address), sales-partner billing (kept separate since it touches real money, same precedent as B2B-04/13), and Known Bugs aggregation (extends B2B-22's hybrid-scope pattern) all remain genuinely not-yet-written and will take whatever ID is next-free when someone actually writes them. Full brief: `.claude/agents/clio/feature-briefs/B2B-26-sales-partner-entity-signup-clients-team.md`. |
| B2B-27 | Card on file required for trial/test-mode access | **Fully built 2026-07-19, migration 087 applied.** `tsc --noEmit` clean, `npm run build` clean (route manifest confirms `/api/admin/billing/card-verification` present), 508/509 tests (same 1 pre-existing unrelated failure) — new dedicated test coverage proving a full fresh 20-minute trial allowance is still blocked (402 `card_required`) with no card on file, while a card-on-file dispatch proceeds byte-for-byte identical to the pre-B2B-27 shape, and live-mode dispatch is completely unaffected either way. | B2B-08 (existing trial/metering gate, extended not replaced) | Per Arun's direct instruction: "Even for trial, they need to add the credit card. Just they won't get charged until trial minutes are over... i dont want the micro charge but definitely need a card on file." Uses a Stripe Checkout Session in `mode: 'setup'` (verifies a card, never charges — Arun explicitly ruled out even a $0 micro-charge) rather than embedded SetupIntent/Elements (confirmed to be retired B2C code, not reused) or any Charge object (Stripe doesn't support true $0 charges on most networks). CEO independently verified this matches every other B2B billing function in `lib/stripe.ts`. Enforcement inserted into the existing `test`-mode branch of `POST /api/partner/v1/sessions`, before trial-minutes computation, with no grandfathering. Deliberately kept as a signal fully independent from `checkStepComplete('payment')`/Go-Live (which still reads only `funding_mechanism`) — CEO confirmed no conflation. Two pre-existing gaps found and deliberately left out of scope, not fixed: no `payment_method.detached` webhook handler exists anywhere (so a card removed via Stripe's own portal goes unnoticed), and a pre-existing inconsistency between `funding_mechanism` and the card-on-file signal. Full brief: `.claude/agents/clio/feature-briefs/B2B-27-card-on-file-required-for-trial-access.md`. |
| B2B-28 | Direct-partner signup becomes invite-only; sales-partner revenue-share tracking & super-admin visibility | **Fully built 2026-07-19, migration 088 applied, committed and pushed (`3c15e82`).** `tsc --noEmit` clean, `npm run build` clean (route manifest confirms `/dashboard/admin/partner-invites`, `/dashboard/admin/sales-partners(/[id])`, `/partner-invite/accept` all present, old `/api/admin/team/sales-partners*` routes correctly untouched), 537/538 tests (same 1 pre-existing unrelated failure) — new dedicated coverage proves the `requirePartnerAdmin` chokepoint and `revenue_share_percent` isolation hold, and the naming-collision fix landed with zero identifier/route/schema drift. Orchestrator independently re-verified `lib/partner/auth.ts` has a literal zero-line diff (not even touched) before pushing — the security-orthogonality guarantee the whole spec depends on held exactly as designed. One real naming collision found and fixed during CEO review (not a nit): the existing internal Team screen's "Sales-partners" panel (B2B-21, Clio's own staff) collided with this brief's new external-reseller "Sales-partners" pages — fixed by renaming the internal panel's copy to "Internal sales staff" at the source (pure string change) rather than patching only the new screen. | B2B-25/26 (done, reverses/extends their signup flow) | Per Arun's direct instruction: "Can we by default assume everyone who signup through getting started as sales-partners. For partners signup, let super admin generate an invite link... single-use link generated on demand... we need to get or enter the profit sharing %. we will not be paying anything for the sales partner... yes drop the question." `/partner-signup`'s Yes/No question removed entirely — self-serve now always produces a sales-partner (`account_kind='channel_partner'`); direct partners (`account_kind='partner'`) become invite-only via a new `direct_partner_invites` table, reusing B2B-21's `lib/internal-admin/invite-tokens.ts` mechanism directly rather than reinventing it. New nullable `revenue_share_percent` column on `partner_accounts` (0-100, meaningful only on sales-partner rows, set only by super-admin, never shown on the sales-partner's own dashboard) — tracked for Arun's own reference, explicitly not an automated payout mechanism ("we will not be paying anything for the sales partner"). New super-admin-facing sales-partner list + per-sales-partner detail view, with a forward-reference-only placeholder card for the still-paused B2B-30 legal-agreement work. Security orthogonality confirmed by direct code read: `requirePartnerAdmin`'s v1.2 chokepoint guard, the Configurator entry-gate, and the DB invariant trigger all gate purely on `account_kind` looked up fresh at request time — a direct-partner row created via this brief's new invite-accept flow is indistinguishable, security-wise, from a historical self-serve row, needing zero modification to any of that hardening. Full brief: `.claude/agents/clio/feature-briefs/B2B-28-direct-partner-invite-only-and-sales-partner-revenue-visibility.md`. |
| — | **B2B-28's revenue-share tracking retired** (2026-07-21) | Per Arun's direct instruction: "drop the revenue sharings. we are not going to do any revenue sharings. so remove all requirements and development on revenue sharings." Migration 091 drops `partner_accounts.revenue_share_percent` and strips its clause from `check_account_kind_invariants()` (088/089's own definitions left untouched — new migration, not an edit to history). `GET /api/admin/sales-partners`/`[id]` no longer select/return the field; the `PATCH /api/admin/sales-partners/[id]` handler (which existed solely to edit this field) removed outright. UI: "Revenue share" column dropped from `SalesPartnersClient.tsx`; the editable share input removed from `SalesPartnerDetailClient.tsx`. Tests: AT-24/AT-26 (both solely about `revenue_share_percent`) removed from `tests/unit/b2b28-security-orthogonality-and-naming.test.ts`; AT-22/23/25/29/30 (unrelated invariants) kept. B2B-28's OTHER scope — invite-only direct-partner signup (`direct_partner_invites`, `/partner-invite/accept`) — is untouched and stays live; only the revenue-share half of that brief is dead. `tsc`/build/tests verified before push. | None (isolated to the one column + its 4 consumers) | |
| B2B-29 | Post-signup onboarding (no pre-signup company capture) & per-client "Configure" screen | **Fully built 2026-07-19, no new migration, committed and pushed (`f3add81`).** `tsc --noEmit` clean, `npm run build` clean (route manifest confirms `/dashboard/channel-partner/settings`, `/dashboard/channel-partner/clients/[id]`, `/dashboard/channel-partner/clients/[id]/configure(+/api,/api/playground,/docs,/known-bugs)` all present), 537/538 tests (same 1 pre-existing unrelated `voice-gap-watchdog` failure). Confirmed by grep: no functional hardcoded `/dashboard/configurator` literal remains in any of the 8 prop-threaded files outside a default-parameter value, and none of the 12 existing direct-partner `page.tsx` callers pass the new `basePath`/`navLabel` props — non-regression for direct partners is structural, not just tested. | B2B-26 (`owning_channel_partner_id`, `requireChannelPartnerAdmin`, `ChannelPartnerShell`, `ClientsClient.tsx`, done), B2B-27 (`partner_wallets.stripe_default_payment_method_id`, `checkCardOnFile`, `createCardVerificationCheckoutSession`, done), B2B-20/23/24 (`ConfiguratorSurface`/`ConfiguratorNavShell`/`VISIBLE_SECTIONS`, done) | Per Arun's direct instruction ("we don't need to start with getting the organization instead can we signup with clerk or regular email and from the dashboard or left pane collect any info... Until payment, user cannot use the bot or send api"), then, on seeing the live flow still asked for company name pre-signup: "yes you need to remove there also. this flow is good" — approving removal from BOTH `/partner-signup` and `/partner-invite/accept`. On the per-client Configurator gap: "yes rename to \"configure\" and modify this screen to align with our ask and enable it for display. you know the drill." **Scope A**: both signup surfaces now go straight into Clerk `<SignUp>`, no interstitial; every new account seeded with the fixed placeholder `partner_accounts.name = 'Unnamed partner'` (BA's resolution, chosen over an email-derived name specifically to avoid leaking a personal email into partner-facing admin lists); welcome-email subject line no longer literally reads "Unnamed partner" (CEO review flagged this edge case by name). **Scope B**: new "Settings" 4th nav tab on `ChannelPartnerShell` with Company-info + Payment (Stripe `setup`-mode card verification, B2B-27's mechanism extended to channel-partner accounts for the first time) panels, non-blocking "Finish setting up your account" checklist banner on the channel-partner Dashboard, and — an explicitly-called-out additive consequence of true component reuse, not scope creep — the same "Company info" card now also appears on the direct-partner Configurator's own Dashboard tab. **Scope C**: new client detail page + client-scoped "Configure" route tree, reusing `ConfiguratorSurface`/`ConfiguratorNavShell`/`ApiClient`/`DocsClient`/`PlaygroundClient`/`PaymentConfigClient`/`IntegrationClient`/`DashboardPanel` **verbatim** (zero fork) via two new optional props (`basePath` default `/dashboard/configurator`, `navLabel` default `'Configurator'`) threaded through exactly those 8 files' 23 hardcoded call sites — the BA's own literal-by-literal sweep of `PaymentConfigClient.tsx` found the CEO review's "six" literal count was actually seven, corrected before build. New `requireChannelPartnerClientAccess()` (`lib/partner/auth.ts`) gates the 6 new page-level routes: resolves the caller's own channel-partner account via `requireChannelPartnerAdmin()`, then verifies the target client's `owning_channel_partner_id` matches it, with the same indistinguishable-403/404 convention this codebase's auth functions already use elsewhere. A single new chokepoint fallback inside `requirePartnerAdmin()` — reached only on the already-failing "no direct membership" path — covers all 43 existing `requirePartnerAdmin`-gated API routes with zero per-route changes, mirroring the B2B-26 §6.14 chokepoint precedent; structurally cannot fire for a channel-partner's own account id (which always has a direct membership row and so still hits the pre-existing B2B-26 403 block first, unchanged). Deliberately did NOT replicate the `onboarding_completed_at`-gated wizard redirect the direct-partner API/Docs/Known-Bugs sibling pages still carry — doing so would gate navigation (not just usage) on a client account, violating this brief's own non-negotiable "payment gates usage, never navigation" constraint. Section 11 (Open Questions) was empty on the approved spec — every BA-discretion point the CEO brief flagged was resolved as a technical decision, none escalated. Full brief: `.claude/agents/clio/feature-briefs/B2B-29-post-signup-onboarding-and-client-configure.md`. |
| B2B-31 | Partner Showcase demo — private "Showcase" tab, LLM topic grouping, real template-pipeline canvas, session-payload panel | **Fully built 2026-07-19, migration 089 applied, committed and pushed (`507b319`).** `tsc --noEmit` clean, real `npm run build` clean (route manifest confirms `/dashboard/channel-partner/showcase`, `/dashboard/channel-partner/showcase/visualization`, `/showcase-render/[visualizationId]`, and all 4 new `/api/channel-partner/showcase/*` routes present), 552/553 tests (baseline 537/538 + 15 new tests, same 1 pre-existing unrelated `voice-gap-watchdog` failure, nothing new broken). Verified `requirePartnerAdmin()`/`requirePartnerApiKey()` have a literal zero-line diff (only additive code appended to the end of `lib/partner/auth.ts`) and all 4 existing `ChannelPartnerShell` callers (`page.tsx`, `clients/page.tsx`, `team/page.tsx`, `settings/page.tsx`) render an identical nav today since the new `showcase_access_enabled` column defaults to `false` for every row. New unit coverage: `requireShowcaseAccess()` access control (blocks non-allowlisted/NULL-flag accounts, admits allowlisted ones), `deriveContentSpecFromExcerpt()`, and the assembled final payload validated directly against the real `CreateSessionSchema.safeParse()`. | B2B-26 (`ChannelPartnerShell`, `requireChannelPartnerAdmin`, `getChannelPartnerAccountForClerkUser`, done), B2B-28 (`revenue_share_percent`/invariant-trigger-extension precedent, done), B2B-29 (`SettingsClient.tsx` dirty-state Save pattern, 4-tab nav base, done), B2B-19 (`CreateSessionSchema`, `ContentPageSchema`, `POST /api/partner/v1/content-sources`, done), B2B-03 (`generateTemplateData`/`selectTemplate`/`TemplateRenderer`, done) | Per Arun's direct instruction, relayed and corrected across two CEO brief revisions (v2.0 supersedes v1.0 outright — a private, persistent, Arun-only tool, not a public/throwaway one): *"this is for sales-admin and it is setup in my dummy account. once the demo is complete i am doing to ask you to turn it off or enable it only for a specific user account"*; *"anything i enter or save should not be deleted or removed until i say so"*; *"in real sales partner scenario, this showcase screen does not exist. instead each sales-partner will directly use our api... using this showcase we can also check our api's are working correctly."* BA spec (`docs/specs/B2B-31-requirement-document.md`, Section 11 empty, no open questions) resolved every CEO-brief-flagged technical decision: a single new `partner_accounts.showcase_access_enabled` column (not a new table, mirrors the `revenue_share_percent`/B2B-28 precedent) gated by a new `requireShowcaseAccess()` (calls `requireChannelPartnerAdmin()` first, then checks the flag, same indistinguishable-403 convention); three new tables (`partner_showcase_content`/`_topics`/`_visualizations`) fully isolated from every real partner-content table, no expiry job of any kind; new demo-only `lib/partner/showcase.ts` (deliberately not `content-generation.ts`) for the LLM topic-grouping call and `deriveContentSpecFromExcerpt()` — a genuine bug fix for `generateTemplateData`'s contentSpec block being silently skipped when `items.length === 0`; the real `selectTemplate`/`generateTemplateData`/`TemplateRenderer` pipeline reused verbatim for each topic's canvas; new public `/showcase-render/[visualizationId]` route with zero Clerk import, mirroring `/partner-render/[clio_session_ref]`'s existing public-by-design precedent exactly; one real `partner_content_sources` row (`auth_type: 'none'`) registered via the same insert shape as the live `POST /api/partner/v1/content-sources` endpoint; and a final read-only panel assembling the exact `CreateSessionSchema`-valid JSON payload for Arun to copy into Postman and fire manually — no new dispatch/proxy mechanism, no rate caps (both explicitly removed from scope once access became private and dispatch became a deliberate manual action). Per the task's explicit instruction, `showcase_access_enabled` was left at its default `false` for every row on push — the Orchestrator grants Arun's own dummy channel-partner account access directly via the documented `clerk_user_id`-scoped SQL pattern (migration 089 + `content-source/route.ts` comments) once the deploy is confirmed live, not as part of this build. Full brief: `.claude/agents/clio/feature-briefs/B2B-31-partner-showcase-demo.md` (v2.0). |
| B2B-32 | Internal Content Test Harness (`test.hello-clio.com`) — hand-authored HTML/image fixture screens, real API dispatch + Postman collection download | **ON HOLD as of 2026-07-22, per Arun's direct instruction ("lets put this on hold for now") — otherwise fully built, deployed, and verified.** Full CEO → BA → Dev chain: Feature Brief (`.claude/agents/clio/feature-briefs/B2B-32-internal-content-test-harness-subdomain.md`), Requirement Document v1.1 (`docs/specs/B2B-32-requirement-document.md`, Section 11 empty — both open questions resolved from Arun's direct answers: indefinite persistence with an explicit Save button, and dual dispatch UX (in-tool "Dispatch now" + downloadable Postman collection, not either/or)), CEO final approval with 8 load-bearing claims spot-verified against actual code before sign-off. Migration 092 (isolated `test_harness_topics`/`_screens` tables + private Supabase Storage bucket, never read by any partner-facing code path) applied live. Committed `18bd0d0`, then `f5b5ae0` fixing a real bug found in live verification (`crypto.timingSafeEqual` — Node-only API — crashed `middleware.ts`'s Edge runtime the moment a well-formed Basic-Auth header was supplied; replaced with a manual constant-time byte-XOR comparator). `tsc`/real `npm run build`/604-605 tests (same 1 pre-existing unrelated `voice-gap-watchdog` failure) clean on both commits. Live end-to-end verification after deploy: 401/401/200 on no-auth/wrong-creds/real-creds, correct rewrite to the Topics page (`x-matched-path: /test-harness`), Topics API returns `{"topics":[]}`, defense-in-depth 404 confirmed on `hello-clio.com`/`www.hello-clio.com` even with valid harness credentials, zero regression on the main app or `distill-peach.vercel.app`. DNS (`test.hello-clio.com A 76.76.21.21`, DNS-only) added directly in Cloudflare; Vercel domain alias + `TEST_HARNESS_HOST`/`TEST_HARNESS_BASIC_AUTH_USER`/`TEST_HARNESS_BASIC_AUTH_PASSWORD`/`TEST_HARNESS_PARTNER_ACCOUNT_ID` env vars set. **Remaining before actual use, paused along with everything else**: one-time `partner_accounts` row provisioned (`f6af5e8f-f595-4fd8-a019-f10cab854ef4`, "Clio Internal — Test Harness", Arun granted `admin` role — not `owner`, since his Clerk identity already owns the HelloWorld account and the partial unique index allows only one `owner` row per `clerk_user_id`), but the test-mode `TEST_HARNESS_PARTNER_API_KEY` was never minted (needs Arun's live session via `/api/admin/partner-keys`) — so dispatch/Postman-download are wired but not yet exercised with a real key. | B2B-19 (`CreateSessionSchema`, `POST /api/partner/v1/sessions`, `safeFetchPartnerPage()` — the real pipeline this harness tests, done), B2B-31 (`ShowcaseContentClient.tsx` dirty-state Save pattern reused, `showcase_access_enabled`-style access-gating precedent considered and deliberately diverged from — Basic Auth chosen instead, single-user tool, done), B2B-05 (Vercel Domains API subdomain machinery — investigated and correctly NOT reused; `test.hello-clio.com` is a static Host-header branch in `middleware.ts`, not B2B-05's dynamic per-partner provisioning, done) | Arun, describing the need directly: *"now i want to create a partner portal separately not part of hello-clio. i will need a web page that has a text box for title, sub-title, text content of the topic, few screens with the visualization content - we can build few with html page and one with image so we can test all those ones out. then with that real information we can pass those details as an api to the bot and lets check if this is working... can we create sub-domain within hello-clio."* Orchestrator recommended a subdomain inside the existing app (not a new deployment) over full separation, reusing existing partner-API/auth/DB infra; Arun approved: *"ok agreed lets build test.hello-clio.com. route through ceo -> ba and build it."* Dispatch-UX answer, verbatim: *"we can fire real api session. also enable a option to download the collection to trigger through postman."* Retention answer, verbatim: *"we can persist with the last entry. have a save button."* Deliberately diverges from B2B-31's Clerk-allowlist access-gating pattern in favor of shared-credential HTTP Basic Auth — reasoned explicitly in the spec as proportionate for a genuinely single-user (Arun-only) internal tool with no partner-account/admin-invite concept, not a shortcut. Pasted-HTML screens are rendered publicly at `/test-harness-render/[id]` behind `Content-Security-Policy: sandbox allow-scripts`, with the authoring side using a sandboxed `srcDoc` iframe (no `allow-same-origin`) rather than `dangerouslySetInnerHTML` — a real security requirement the CEO brief flagged explicitly, not left to build-time improvisation. Full brief + spec history above; no further action until Arun lifts the hold. |
| B2B-19 | Inline content delivery (partner URLs + auth) + transition marker + minute enforcement | **Fully built 2026-07-17, migration 083 applied, committed and pushed (`23a66df`).** | B2B-02 (partner API), B2B-04/13 (wallet), B2B-11 (prompt assembly precedent) | Resolves the architectural fork from the 2026-07-17 core-objective restatement: new additive, backward-compatible session contract for partner-hosted HTML/image URLs + per-page transition triggers (Option 2 template/Designer path untouched). Content-source auth: 3 types built (none/static-bearer/OAuth2-to-partner's-server) via a two-step API-only registration (`POST /api/partner/v1/content-sources` → opaque `content_source_id`, AES-256-GCM encrypted-and-retrievable not hashed, since Clio replays it outward) + 2 documented-but-rejected (pre-signed URL, mTLS). Transition marker grounded in existing RTV-02/03 golden-word + forward-only transcript-watching machinery, not invented fresh. Minute enforcement: confirmed at source that live-mode init only checks for a payment method (not `balance_usd`) and billed minutes come from wall-clock/render-client timing, not Attendee's actual join/leave data — both real gaps to close. **3 open questions needing Arun's direct answer** (per the known CEO-relay limitation, routed directly, not through Orchestrator relay): (Q-A) allow inline public/no-auth sources without the registration step? (Q-B, the big one) does page-transition fire from server-side transcript detection — making this the first place transcript-watching is authoritative over a live screen in production, ahead of the still-deferred RTV-05 — or stay on bot-self-invoked `show_visual` tool calls? (Q-C) exact minute-exhaustion threshold, and is a mid-session force-end wanted or initiation-block only? Full brief: `.claude/agents/clio/feature-briefs/B2B-19-inline-content-delivery-transition-and-minute-enforcement.md`.
| B2B-18 | Retire B2C signup chain + dead-link cleanup | **Fully built 2026-07-17.** ID collision resolved (3-way B2B-17 clash — see changelog), brief file renamed, all cross-references fixed. Retired `app/onboarding`, `app/topics`, `app/plan`, `app/checkout`, `app/(marketing)/pricing` (`lib/onboarding.ts` and `components/kb/*` correctly KEPT — live importers confirmed). Clerk redirects now target `/partner-signup/organization`. `MarketingNav`'s "Pricing" link removed. Homepage hero copy swapped to accurate API-driven positioning (`PhoneMockup` deliberately untouched, deferred to the frozen redesign item). `DashboardShell` nav trimmed to exactly Clients/Templates/Glitches. `/admin/seed` deleted. `tsc`/build clean, 469/470 tests (1 pre-existing unrelated failure). **New orphan candidates surfaced, not deleted**: `app/api/topics/{recommendations,generate}/route.ts`. **Stale links found outside this brief's named scope, deliberately left**: `lib/delivery/email.ts` (~12 refs to deleted `/dashboard/plan`/`/sessions`, marked "shared engine — untouched"), `WalkthroughClient.tsx:1635`, and 4 Playwright e2e tests that navigate to now-404 B2C routes (will fail if that suite runs against a live server; not part of `vitest`). Not yet committed.
| B2B-14 | Remove confirmed-dead B2C dashboard surface | **Confirmed-safe subset fully built and independently verified 2026-07-16.** Deleted: `app/dashboard/DashboardClient.tsx`, `app/dashboard/upgrade/`, `app/dashboard/welcome/`, `app/dashboard/billing/`, `app/dashboard/messages/`, 4 dashboard components (`ScoreRing`/`StreakCounter`/`DeliveryToggle`/`MessageCard`), 4 orphaned API routes (`app/api/messages/route.ts`, `messages/[id]/feedback/route.ts`, `checkout/activate/route.ts`, `portal/route.ts`). `app/dashboard/page.tsx` replaced with a single-purpose `redirect('/dashboard/configurator')`. `DashboardShell.tsx`'s nav cleaned (both dead entries + unused icon imports removed; mobile nav reshuffle verified to surface a real live page, `/dashboard/phone`). `middleware.ts` and sign-in redirect fixed. `tsc --noEmit` clean, `npm run build` clean, 475/476 tests. Not yet committed. **Two items explicitly pulled out of scope, awaiting Arun's direct answer, not resolved**: (1) whether "session" in his instruction means the individual end-user session-viewing pages only or the shared voice-coaching engine underneath (also used by B2B-09/10/11's partner-session machinery); (2) `app/plan/`/`app/checkout/`/the pricing page are load-bearing — `app/dashboard/layout.tsx` gates all of `/dashboard/*` (including the live Configurator) on an active-subscription check that redirects to `/plan`, so these can't be deleted without first deciding what replaces that gate. | B2B-02/03 (done, live surfaces this brief explicitly does not touch) | Arun's exact instruction from live testing: "you need to change and remove to align with our new goals. plan, session etc are not needed anymore. modify the dashboard." Confirmed direct decision, not a discussion item — closes task #44. CEO brief scoped only the unambiguous "plan" (Starter/Pro/Executive tier picker) cluster for deletion and explicitly excluded the "session" cluster pending Arun's confirmation, rather than guessing on something that could break currently-live, actively-developed infrastructure. BA's independent re-verification (per the brief's own instruction not to trust its list as exhaustive) found the confirmed-safe list mostly held up, but pulled `app/plan/`/`app/checkout/`/pricing page OUT after discovering they're load-bearing for `app/dashboard/layout.tsx`'s subscription access gate — a real, potentially product-breaking dependency neither the original QA finding nor the CEO brief had caught. Two CEO review cycles caught real, purely-mechanical gaps (a second dead nav link that would have shipped, two more orphaned routes) before final approval — both fixed and re-verified against live files, not just trusted from BA prose. |

### Infra Migration to Mia Digital LLC (parallel track)

| ID | Item | Status | Notes |
|----|------|--------|-------|
| INFRA-01 | GitHub repo transfer | Not started | Built-in transfer preserves history/issues. |
| INFRA-02 | Vercel team + project | Not started | Depends on INFRA-01 (import repo into new team). |
| INFRA-03 | Clerk application | Not started | Scoped to partner-admin auth only, no consumer sign-up. |
| INFRA-04 | Supabase project | Not started | Fresh migrations, no data carryover (B2C retiring). |
| INFRA-05 | Stripe business account | Not started | Business verification (EIN, bank account) — slowest step, plan for 1-3 business days. |
| INFRA-06 | Cloudflare account-to-account transfer (`hello-clio.com`) | Not started | Domain confirmed registered via Cloudflare; account-to-account transfer, not a registrar transfer. Arun confirmed OK to proceed (member-add rejected as insufficient — full transfer needed). |
| INFRA-07 | Brand/domain final decision | In progress | Leaning `hello-clio` — domain already owned, brand-fit concern resolved (not customer-facing), trademark risk assessed as moderate not blocking (Themis Solutions' CLIO mark is narrowly scoped to legal practice management; crowded field of other coexisting CLIO marks). Recommended: confirm with a trademark attorney in parallel with LLC formation, not a hard gate. |

---

## Changelog

- **2026-07-19**: Per Arun's direct instruction ("also remove all user and organization entries lets start with a clean slate"), the Orchestrator wiped all 7 test `partner_accounts` rows and their dependents (all confirmed live-testing artifacts from 2026-07-18, none real customer data — names: "Pizzaboy," "helloworld" x2, "arun's Organization," "hello's Organization" x2) ahead of B2B-25. `internal_admin_users` (Arun's super-admin seed) was explicitly preserved, not part of this wipe. The corresponding Clerk-side Organizations still need manual removal from the Clerk Dashboard (Supabase can't reach Clerk's API without credentials this session never handled): `org_3Gf3Y3qWaEFeXG9Pu9odjcchElE`, `org_3GeaNioVRbhRRlaItTS6aP5u1Oj`, `org_3GeaOmw8xE0tKkTLX6vGjEjgovt`, `org_3GgZFfxs4tin0NBq4OU7ufhmIup`, `org_3GgZH5tEt44TAvkG5vFSwyf4MQE`, `org_3GhHLoMnwyN2kZOTytnlC36sfEt`, `org_3GhHMyni6fc6LDMR3dnf2xkxwyL`. A separate stray row in the legacy `users` table (Arun's own account, `hello.arunprakash83@gmail.com`, created 2026-07-16) was left untouched pending Arun's call on whether to remove it — unrelated to the Organizations mechanism, different table.
- **2026-07-13**: File created. Orchestration approach captured. B2B-01..05 and INFRA-01..07 scaffolded from `docs/brainstorm-b2b-platform-pivot.md`. F-01/F-02 marked in progress (session tasks #1 opened to resolve). Archive branch `archive/b2c-legacy` created and pushed to `origin` prior to this work starting.
- **2026-07-13 (later same day)**: Removed 10 stale B2C-era root docs (`TASKS.md`, `brief.md`, `CORE_OBJECTIVES.md`, `CLAUDE_CODE_BUILD_PROMPT.md`, `test-report.md`, `STORIES.md`, `PROJECT.md`, `architecture.md`, `research-findings.md`, `DECISIONS.md`), extracting COGS data from `DECISIONS.md` into F-02 first. Rewrote project `CLAUDE.md` to drop the old B2C agent roster/design system. Updated stale memory pointers. Added V-01 (ElevenLabs → Hume, decided, removal dispatched to background agent) and V-02 (Recall.ai → Attendee, under evaluation, not decided — do not touch Recall code).
- **2026-07-13 (later still)**: V-01 marked **Done** — ElevenLabs removal complete (7 files deleted, 36 modified, clean build/typecheck, tests passing), relay-mode deletion confirmed safe by Arun (`MEETING_BOT_AUDIO_MODE` not set in Vercel). Committed as `d49754c` (B2C/docs cleanup) and `7a0020a` (ElevenLabs removal). Two non-blocking follow-ups open: manual `.env.local.example` cleanup (permission-blocked for both agent and Orchestrator), and a pre-existing latent silence-detection bug surfaced but deliberately not fixed as part of this task. V-02 (Attendee) researched: real-time bidirectional audio confirmed supported (the one non-negotiable capability), hosted/cloud option confirmed in use (no self-hosting), pricing ($0.50/hr, volume discount to $0.35/hr) looks cheaper than Recall.ai's old $0.0108/min rate, platform coverage (Zoom/Meet/Teams) matches.
- **2026-07-13 (later still)**: V-02 code-coupling audit complete (read-only, no code changed) — verdict: NOT a flag flip. A partial `MeetingBotProvider` abstraction exists and `attendee.ts` is a real working implementation with evidence of live testing, but 3 things bypass it: the production cron that deploys bots before scheduled sessions hardcodes Recall.ai directly, the Attendee webhook handler is a feature-incomplete parallel implementation (missing sentiment/deferred-question/ice-breaker handling), and post-session quality evaluation + RTV-03 accuracy reporting are hardcoded to Recall's transcript format with no Attendee equivalent (would silently fail, not loudly). 6-item concrete fix list captured in the V-02 row. Full migration not authorized yet — awaiting Arun's call on full-fix-first vs. staged/dual-provider vs. hold.
- **2026-07-13 (later still)**: Arun authorized "full migration." Phase 1 dispatched and completed (uncommitted) — see V-02 row for full detail. **Major correction discovered mid-phase**: Attendee+Hume was already the live production config since 2026-07-04 (Orchestrator's "under evaluation" framing was wrong — should have checked `docs/brainstorm/` first). Flagged to Arun: a production data-quality concern (possible 9 days of sessions with silently-missing sentiment/deferred-question/quality-eval data, backfill not yet scoped), 2 unapplied Supabase migrations blocking deploy, and a much larger undecided architecture item (Hume-native-LLM + mega-prompt + Chat-History-API transcript pull + PDF export) from the same brainstorm doc, explicitly gated behind Arun's standing "spec before build" rule and not built as part of this phase. Arun's reply ("ok") was not treated as authorization to proceed on the native-LLM item given the stakes — still awaiting an explicit answer.
- **2026-07-13 (later still)**: Arun walked through the Hume-native-LLM architecture Q&A directly — confirmed prompt-size OK, visualization triggering is server-side transcript-watching (not Hume tool-calling), `end_session` IS Hume-tool-driven (mitigated by a recommended timeout backstop), PDF export removed from scope entirely (now the partner's responsibility under the pivot, not deferred). `docs/brainstorm/ATTENDEE-HUME-ARCHITECTURE-brainstorm.md` updated accordingly. Arun authorized proceeding straight to a CEO Agent Feature Brief (skipping a pre-spec spike, since both original test-only unknowns were resolved by direct confirmation).
- **2026-07-13 (later still) — SECOND MAJOR CORRECTION**: dispatched two CEO Agents in parallel (Hume-native-LLM brief, B2B-01 Core Objectives rewrite). The Hume-native one came back having actually verified the live codebase rather than trusting the 9-day-old brainstorm doc, and found **most of the described architecture is already built and shipped**: native-LLM mode switch (`HUME-NATIVE-01`, shipped 2026-07-05, `NEXT_PUBLIC_HUME_NATIVE_ENABLED=true` in Production/Preview), full profile+intent injection into the upfront prompt, a real max-session-duration timeout backstop (`inngest/session-timer.ts` — the thing Orchestrator had just recommended as new, unaware it already existed), and `end_session` as a Hume-native tool call (`SESSION-END-01`, shipped 2026-07-09). Real remaining gaps, scoped into a new brief (`.claude/agents/clio/feature-briefs/HUME-NATIVE-02-transcript-visualization-and-action-items.md`, CEO Agent deliberately did not auto-spawn BA given the scope divergence): (1) visualization is still Hume-tool-call-driven today, the opposite of Arun's confirmed intent — needs to be flipped to server-side transcript-watching; (2) post-session action-item/glitch extraction doesn't exist at all. **Separately, a real production blocker surfaced**: `SESSION-END-01` shipped without one manual step (`docs/session-end-01-hume-setup.md` — 2 Hume Tools API curl calls requiring the real, non-placeholder `HUME_API_KEY`), unresolved since 2026-07-09 — until run, Hume-native sessions end via a fallback timer, not real tool-calling, so `end_session` reliability can't even be observed yet. **Process gap owned by Orchestrator**: `.claude/agents/clio/feature-briefs/` and `requirement-docs/` contain 25+ prior CEO/BA artifacts (RTV series, TMPL series, SESSCTX-01, ONDEMAND series, `FB-HUME-GROUND-TRUTH-01-elevated.md`, etc.) spanning weeks that were not surveyed before this session's work began — should be checked at the start of future related work, not discovered mid-agent-run.
- **2026-07-13 (later still)**: B2B-01 fully approved and closed — `CORE_OBJECTIVES.md` v2.0 written to repo root (not yet git-committed), B2B-02 unblocked. HUME-NATIVE-02 BA spec done, another instance of the same process gap: **Part A's mechanism already has an approved-but-unbuilt spec, RTV-05** (`prefetch-and-dual-trigger-display.md`, APPROVED 2026-07-10) — the CEO brief never referenced it. BA also caught and reversed a real bug in the CEO brief's own recommendation (removing `show_visual`/`advance_tab` from Hume's tools would have silently broken content delivery, since those tool responses are also how Hume receives its next teaching script, not just a screen-timing signal). Part B (action-item/glitch extraction) is fully spec'd, zero open questions, dev-ready. Part A has one genuine open question RTV-05 surfaces — its own per-template rollout gate (0/27 templates currently approved) means building the mechanism doesn't make it live in production — flagged by the BA as needing to go to Arun directly, not through CEO Agent, per the known relay limitation. Awaiting Arun's answer.
- **2026-07-13 (later still)**: B2B-02 CEO brief written (`.claude/agents/clio/feature-briefs/B2B-02-partner-api-multi-tenant-architecture.md`), grounded in `CORE_OBJECTIVES.md` v2.0 as primary source per Arun's instruction that it supersedes the brainstorm doc's phrasing, plus a full read of `docs/brainstorm-b2b-platform-pivot.md` and this file. Scope: partner-level API keys, content/profile push-pull contract, usage ledger + signed webhooks, sub-tenant hierarchy, plus a session-initiation contract added as an implicit necessity (not explicitly named in Arun's original list but required for the other four to have an entry point). Made explicit that this brief introduces a *second*, new auth system (partner API keys, machine-to-machine) distinct from Clerk's existing partner-admin scope, and that the Clio→partner direction (content/profile pull, usage webhooks) is authenticated by partner-supplied credentials, not a Clio-issued key — flagged as a likely conflation risk. On F-01 (ledger storage, still open): judged the brief scopable without resolving it first, mirroring how `CORE_OBJECTIVES.md` itself was written to hold under either resolution — instructed BA to spec a webhook-dispatch log as F-01-independent, document both resolution branches explicitly, and escalate back only if a true architectural fork (not just a storage/query difference) surfaces. Not escalated to Arun. Next: dispatch BA Agent for the Requirement Document.
- **2026-07-13 (later still)**: B2B-04 CEO Feature Brief written
  (`.claude/agents/clio/feature-briefs/B2B-04-billing-metering.md`), grounded in
  `CORE_OBJECTIVES.md` v2.0 Objective 6, `docs/brainstorm-b2b-platform-pivot.md`
  §7.1–7.6/§8, this file's F-01/F-02 resolutions, `docs/specs/B2B-02-requirement-document.md`
  Section 6, `architecture.md` Section 8, the live `usage_events`/`partner_accounts`/
  `webhook_dispatch_log` schemas (migrations 071/072/074), the existing `GET
  /api/partner/v1/usage` route, the `/dashboard/admin/templates` admin-page precedent, and
  current (B2C-era) `lib/stripe.ts`. Scope: single unified wallet metered per
  `usage_events.event_type` (a superset of Arun's originally-named 2 categories, since B2B-03
  already extended the ledger to 8 sub-types), versioned burn rates (so a future F-02 rate
  change never reprices historical usage), Stripe funding mechanism chosen per tier
  (Checkout one-time top-up / auto-recharge Subscription / Invoicing) rather than Stripe's
  native metered-billing API (doesn't fit a prepay model), both the internal admin page and
  partner-facing dashboard, and closing the `usage_events` idempotency gap `architecture.md`
  had flagged as "moot" pre-billing (no longer moot once real money is decremented per row).
  Two items resolved by CEO without escalating to Arun — see brief for full reasoning. Next:
  dispatch BA Agent for the Requirement Document.
- **2026-07-13 (later still)**: B2B-02 shipped full chain (BA spec → CEO approval → migration `071` applied → built: `lib/partner/*`, `/api/partner/v1/*`, admin key management, HMAC-signed webhook dispatcher, `/partner-render/` placeholder). Arun then answered all 5 outstanding questions in one batch: **F-01 resolved to Option A** (his own words: "no user details will be saved... we only take relevant information like meeting duration etc and update our counter, create logs on what topic, session id, duration so the customers can verify from their end as well... probably client id as well for each customer") — migration `072` (`usage_events` ledger) applied, matched his description closely already; follow-up build made it the permanent unconditional path (was previously feature-flagged pending this decision), with 2 partner-visible behavior notes flagged (`session.completed` now returns an empty page on the usage endpoint since it's non-billable; `test_mode` events are now hard-filtered out) and one latent gap noted for later (no idempotency guard yet on the ledger insert itself, currently moot since no real call sites exist). **F-02 deferred** — proceed with stale numbers, backlog created to finalize real ones once the build is complete. **Backfill: declined** — forward-only from now, no investigation into pre-existing sessions. **B2B-03 scope locked**: customer/partner-facing, NOT end-user-facing, with a hard multi-tenant isolation requirement ("each partner should be isolated and modular (parameter driven) from their configuration screen") — written into this tracker so B2B-03's eventual CEO brief doesn't have to guess, unlike the earlier Template Designer scope gap. **`SESSION-END-01` fully completed**: real `HUME_API_KEY` and `NEXT_PUBLIC_HUME_CONFIG_ID` provided, both Hume Tools API steps run directly (not delegated — required a real production credential no agent could hold). Along the way: the `end_session` tool already existed from an earlier partial attempt (POST returned 409 Conflict); fetched its real id/version via the tools-list endpoint instead of trusting the setup doc's guess (which had wrongly speculated that same tool ID belonged to `defer_question`); the base config's update endpoint required the full config body, not a partial patch (undocumented in the setup doc) — reconstructed it in full from a live GET to avoid corrupting the production voice config, then POSTed. Verified live: config version 18, tools = `[advance_tab, show_visual, end_session]`. Updated the fallback array in `config-provisioner.ts` with the real tool id/version. All V-01 and F-01 follow-up items are now closed except the two still explicitly deferred to Arun (`.env.local.example` manual cleanup, RTV env var documentation).
- **2026-07-13 (later still)**: B2B-04 shipped full chain. BA spec (`docs/specs/B2B-04-requirement-document.md`) went to CEO review and was sent back once, with two precise, non-rejecting gaps: (1) the `days_remaining` sort tie-break rule for the two structurally different situations that both produce `projected_days_remaining: null` (a negative-balance/exhausted account vs. a zero-usage account) was referenced but never actually written down; (2) `avg_daily_burn_usd`/`projected_days_remaining` had no defined formula (window length, aggregation, partial-day handling all unspecified) despite being the number the entire "at-risk accounts surface without manual checking" value proposition depends on. BA closed both in a targeted revision (v1.1): added a `days_remaining_null_reason: "exhausted_balance" | "no_burn_rate" | null` response field plus a signed-infinity sort comparator (`architecture.md` §13.6) so both the default sort and the column-header re-sort reuse one comparator, never scrambling the two null meanings; pinned the burn-rate formula to a trailing-7-complete-UTC-day simple mean with the current partial day always excluded, never prorated (`architecture.md` §13.5). CEO re-reviewed against the actual document text (not the BA's summary) and approved. Migration `075` applied (`partner_wallets`, `billing_rate_versions`, `wallet_ledger`, `usage_events` extensions + idempotency unique index). Build dispatched and completed: wallet-decrement mechanism, all 3 Stripe funding paths (Checkout top-up / Subscription auto-recharge / Invoicing) reworking the B2C-era `lib/stripe.ts`, the `lib/partner/webhooks.ts` idempotency fix that was the real bug motivating this brief (a webhook retry no longer double-writes `usage_events`/double-decrements the wallet), the `/dashboard/admin/clients` operational screen, and the new `GET /api/partner/v1/wallet` partner-facing endpoint. `tsc --noEmit` clean, test suite passes (one pre-existing unrelated failure, verified via `git stash` that it predates this work). One genuine gap the build agent found and fixed proactively: `webhook_dispatch_log.event_type`'s CHECK constraint (migration 071) didn't include `'wallet.low_balance'`, which the approved spec explicitly specifies dispatching — an additive follow-up migration widened the constraint rather than leaving that dispatch permanently and silently failing. Not yet committed.
- **2026-07-13 (later still)**: CEO Agent wrote the B2B-05 Feature Brief (`.claude/agents/clio/feature-briefs/B2B-05-domain-whitelabel-infra.md`). While researching, found and corrected **two stale Live Status rows**: B2B-03 was marked "CEO brief written, pending BA dispatch" but was actually fully built and committed (`a49e151`); B2B-04 was marked "built, uncommitted" but was actually committed (`a49f1dc`, `34f7e92`) — both confirmed via `git log`, not just the tracker's own prior text. Verified against live code (not just the brainstorm doc) that domain/subdomain routing is genuinely unbuilt: `middleware.ts` has no Host-header logic, `partner_accounts` has no `subdomain_slug`/`custom_domain` columns, and `docs/specs/B2B-03-requirement-document.md`'s own Out-of-Scope section explicitly deferred both to B2B-05 by name — confirming brainstorm §7.5's subdomain-first + Vercel Domains API + Host-header-middleware recommendation as real, correctly-scoped, unblocked work. **Scoped the onboarding wizard (§7.6) out of this brief's approved deliverables** — unlike §7.4 (wallet model), §7.6 never received an explicit Arun confirmation in `docs/brainstorm-b2b-platform-pivot.md` §8 item 4, and it was never one of Arun's own stated requirements (Section 1) — it originated as an Orchestrator suggestion. Per Product Principle #2 ("Ambiguous UX = STOP") and the CEO's standing instruction not to guess on product-shape calls, this needs Arun's direct yes/no rather than a CEO judgment call; not treated as a blocker on the rest of the brief since B2B-03 already ships the "no wizard, directly-editable Configurator" fallback live. Domain/subdomain mechanism itself is ready for BA dispatch now.
- **2026-07-13 (later still)**: Arun asked why `getPlanFromPriceId` was kept alongside the B2C-era webhook branches instead of being removed — pointed out that if the code is being reused it should be renamed to B2B terms, not left labeled B2C. On inspection: it wasn't actually being reused for anything B2B — it was pure dead code, the last of 5 dead top-level `switch` branches (`customer.subscription.created/updated/deleted`, `invoice.payment_failed`, `customer.subscription.trial_will_end`) plus 2 dead sub-branches inside `checkout.session.completed` (`type: 'topup'` minutes purchase, the subscription-fallback handler) — all tied to the retired per-user `users` table flat-plan model. Removed all of it from `app/api/webhooks/stripe/route.ts` and deleted `getPlanFromPriceId` from `lib/stripe.ts`; the route now only handles B2B-04's 5 wallet-funding/payment-method-sync events. Checked call sites before removing anything: `sendSMS`, `writeMinutesLedgerEvent`, and `sendTrialEndingEmail` are still live elsewhere (SMS reminders, session-end billing, `inngest/trial-expiry.ts`) — those files were left untouched, only the dead call sites inside the Stripe webhook were removed; `sendWelcomeEmail`/`sendPaymentFailedEmail` in `lib/delivery/email.ts` are now orphaned (no remaining caller) but were left in place since deleting them is a different file/different scope than what was asked. Updated `tests/unit/stripe.test.ts` to drop the `getPlanFromPriceId` suite. Along the way, found and excluded 8 stale `.claude/worktrees/` directories (frozen at old commits, unrelated to any current work) from `vitest.config.ts` — they were causing 60 false test failures from duplicate copies of pre-B2B-04 test files. Post-cleanup: `tsc --noEmit` clean, 384/385 tests pass (the 1 remaining failure is the same pre-existing, unrelated `voice-gap-watchdog.test.ts` issue already noted above).
- **2026-07-13 (later still)**: B2B-05 shipped full chain — the fifth and final Feature Brief in the original pivot sequence. CEO wrote the brief after finding, mid-research, two stale tracker rows (B2B-03/B2B-04 both marked incomplete when they were actually already committed) and correcting them inline. CEO explicitly declined to guess on one genuine product-shape question the brainstorm doc had left unconfirmed since 2026-07-12 (whether to build an onboarding wizard) and asked Arun directly rather than assuming; Arun confirmed "build it now" as the full 7-step guided flow, not a fast-follow or lightweight version. BA wrote the core domain-infra spec first, then a second BA pass extended it with the wizard (v1.2). CEO review caught one real implementation gap before approval — the wizard's plan to reuse 4 existing Configurator screens via an `embedded` prop assumed each had one return statement, but two of them (`QuestionnaireBuilderClient`, `ContentConfigClient`) actually have two separately-wrapped branches each; a literal implementation would have shipped double UI chrome or a broken embedded state. BA fixed it with independent per-branch duplication (verified against the live files), CEO re-reviewed and approved. Migration `076` applied. Build agent shipped the full scope — subdomain/custom-domain infra via the newly-approved `@vercel/sdk`, host-header tenant resolution extending `middleware.ts`'s existing Clerk gate, and the 7-step wizard — and along the way found and fixed a real, unrelated pre-existing bug: B2B-03's public partner-questionnaire route was missing from `middleware.ts`'s public-route list, meaning real unauthenticated end users hitting it would have been redirected to sign-in. `tsc --noEmit` clean and 384/385 tests pass, independently re-verified by Orchestrator before reporting. Not yet committed. This closes B2B-01 through B2B-05 — the full originally-scoped B2B pivot build.
- **2026-07-13 (overnight)**: Arun asked for a full completeness audit before going to sleep — "check that the user will be able to navigate to all the screens... and perform all the expected actions," explicitly authorized to proceed without waiting for approval. Ran 5 parallel read-only audit agents across the B2B partner-admin surface (Configurator hub navigation, admin/billing pages, public partner-facing pages, the B2B-05 wizard step-by-step, and a cross-cutting dead-link/stub sweep). Findings and fixes, all verified with a fresh `tsc --noEmit` + full test suite pass afterward (384/385, same pre-existing unrelated failure):
  - **Critical: wizard Payment step could permanently strand a partner-admin.** Two compounding bugs in the just-shipped B2B-05 wizard: (1) the self-serve "Pay as you go" Stripe webhook path never wrote `partner_wallets.funding_mechanism` (the DB's own CHECK constraint anticipated `'checkout_topup'` as a value, but nothing ever wrote it) — the wizard's payment-step completion check could never pass for that funding path, no matter how many times a partner paid; (2) even the working subscription path left the client's `viewStep` state stuck on `'payment'` after a successful return from Stripe, because the shared `load()` helper's `prev ?? data.current_step` guard only fills a *null* viewStep — a real user would see no error, no visible progress, and the only way out was an un-hinted browser refresh. Fixed both: the webhook now writes `funding_mechanism: 'checkout_topup'`; the `funded=1` return handler and both `onContinue`/`onSkip`'s 409 branches now force `viewStep` to the server's fresh `current_step` instead of leaving it stale; added a clickable "Go live" step-indicator node as a manual escape hatch once reachable.
  - **3 dead-link/UI bugs in Configurator screens**: `QuestionnaireBuilderClient` and `ContentConfigClient` had `backHref="#"` placeholders (non-functional back arrows) in their edit/review branches, and `QuestionnaireBuilderClient`'s top-level list view had no `backHref` at all (no back arrow rendered, inconsistent with every sibling screen). All 3 fixed to point back to the Configurator hub, matching every other screen's pattern.
  - **1 genuinely broken link, unrelated to the B2B work**: `app/dashboard/knowledge-base/KBIndexClient.tsx`'s empty-state CTA linked to `/dashboard/schedule`, which doesn't exist (real route is `/dashboard/schedule-setup`) — a real 404 for any signed-in user with zero KB topics. Fixed.
  - **Admin-page navigation gap**: `/dashboard/admin/clients` (B2B-04) and `/dashboard/admin/templates` were confirmed disconnected islands — no shared admin layout/sidebar, no cross-links, reachable only by typing the exact URL, and not linked from `/dashboard` itself either. Added a minimal cross-link between the two pages' headers rather than building a full admin shell (out of scope for an overnight fix).
  - **Dormant middleware gap, fixed proactively**: `PartnerRenderClient.tsx`'s same-origin calls to `/api/hume-token` and `/api/partner/render/end-session` were missing from `middleware.ts`'s `TENANT_SCOPED_PATTERNS` — currently unreachable in production (partner-render URLs are always built from `NEXT_PUBLIC_APP_URL`, never a tenant host, per `app/api/partner/v1/sessions/route.ts`), but would silently break voice connection and end-of-session wallet accounting if that ever changes. Added both patterns before the gap could ever actually fire.
  - **Stale doc comment fixed**: `lib/partner/render-data.ts`'s module comment still said B2B-03 render-path wiring was "explicitly B2B-03's job" (i.e. not yet done) — it's been done since B2B-03 shipped (`lib/partner/live-render.ts` calls these functions directly). Comment updated to reflect reality.
- **2026-07-15**: B2B-08 (Testing/Metering) CEO Feature Brief written
  (`.claude/agents/clio/feature-briefs/B2B-08-testing-metering.md`), dispatched off the same
  `docs/brainstorm-partner-signup-integration.md` session that produced B2B-06 v2 and B2B-07. ID
  confirmed as the next free number (`B2B-07` already reserved for the developer portal per the
  brainstorm doc's own "Brief breakdown" section). Grounded in Arun's own words ("We can give them
  20 minutes of free bot usage for testing then they can pay for 2 hours every time to continue
  testing" / "That cost is on us. We will send real bot.") plus a direct code read confirming
  `dispatchMeetingBot()` does not branch on `test_mode` today — meaning a `test`-mode key dispatch
  is *already* a real, unbounded, unmetered, real-cost Attendee+Hume bot with zero cap, independently
  cross-confirmed by the B2B-07 row's own pre-condition note. This reframes the brief as bounding an
  already-live gap, not building a mechanism from nothing. Resolved all 6 items the dispatching task
  posed, each with the reasoning shown (not just an answer) so BA can verify the logic: lifetime
  (not per-session) allowance tracking on a new `partner_wallets.trial_minutes_used` column; a real
  server-side mid-session forced cutoff (new `inngest/partner-trial-cutoff.ts`, modeled on the
  existing `session-timer.ts` precedent, calling Attendee's existing outbound `leave` endpoint) since
  an end-of-session-only check cannot actually bound Clio's cost exposure; the 2-hour block funded via
  a real $1.80 one-time Stripe charge into a **new, separate** `test_minutes_balance` counter rather
  than the production `balance_usd` (two code-confirmed reasons: `applyWalletDecrement()` already
  unconditionally skips `test_mode=true` events, and conflating test spend into `balance_usd` would
  corrupt the admin page's real-revenue reporting); the $1.80 rate itself derived from the existing
  seeded `voice_minute` COGS-placeholder rate (120 min × $0.0150/min), no new figure invented; a new,
  purely-additive, Clio-internal-only `usage_events.is_metered_test_usage` flag added *alongside* the
  unchanged `test_mode` flag, resolving the brief's own flagged tension that `test_mode`'s existing
  "never billed, permanently" meaning cannot just be reused for usage that does eventually cost real
  money; and confirmed orthogonal-by-activation-condition to B2B-06's live-mode-only funding
  guardrail (this brief's gate fires only for `test`-mode keys), with one named non-required positive
  interaction (`setup_future_usage: "off_session"` on the block-purchase Checkout session means a
  partner's first test-block purchase can incidentally satisfy B2B-06's payment-method check for
  their later live-mode usage). All schema changes additive; explicitly excludes the separate
  Attendee inbound-webhook signature bug, a partner-facing purchase UI (flagged as a real B2B-03
  follow-on gap, not built here), and retroactive billing for historical unmetered test usage
  (forward-only, matching F-01's own declined-backfill precedent). Zero open questions. Does not
  block on B2B-06 landing first. Next: dispatch BA Agent for the Requirement Document.
  - **Confirmed, not fixed (by design or out of scope for tonight)**: `/dashboard/configurator/wizard` is reachable only via the automatic onboarding-gate redirect or a direct URL, never a hub menu item — this is intentional (it's a gate, not a hub destination) but means there's no "resume setup" affordance if a partner-admin navigates away mid-wizard; `/api/admin/billing/invoice` (enterprise) has no UI trigger anywhere, confirmed intentional/ops-only per its own code comment; `app/dashboard/messages/MessagesComingSoon.tsx` is dead/orphaned code (the real `MessagesClient` is what actually renders) — left in place, flagged for a future cleanup pass rather than deleted without being asked.
  - **The most significant finding — confirmed with full certainty, not new tonight but re-verified exhaustively**: there is no code path anywhere (`grep`-confirmed across every `.insert()` call in `app/`, `lib/`, `inngest/`, every migration, every seed script) that creates a new `partner_accounts` or `partner_admin_users` row. Every partner-lifecycle feature built across B2B-02 through B2B-05 assumes the account already exists. This is being driven as its own Feature Brief — see the new B2B-06 row below.
- **2026-07-13 (overnight, continued)**: CEO Feature Brief written for the B2B-06 gap
  (`.claude/agents/clio/feature-briefs/B2B-06-partner-provisioning.md`), grounded in a full read of
  migration `071`'s live schema (both tables' exact NOT NULL/CHECK constraints), `lib/partner/wizard.ts`,
  `lib/partner/webhooks.ts` and migration `075`'s `credit_wallet_balance`/`decrement_wallet_balance`
  SQL functions, `middleware.ts`, `app/api/webhooks/clerk/route.ts`, and
  `docs/specs/B2B-05-requirement-document.md` §13. Scoped narrower than the gap first appeared: both
  `partner_wallets` and `partner_onboarding_progress` already self-provision (confirmed in code, not
  assumed) the moment an account exists, and `POST /api/admin/partner-keys` already works once a
  `partner_admin_users` row exists — so the brief is two new routes (create account, link an existing
  Clerk user as admin by email) plus a list route, no migration. Four audit questions resolved directly
  by CEO rather than escalated, each grounded in an already-approved precedent from a prior brief
  (B2B-04's enterprise-negotiated-not-self-serve provisioning model; B2B-04's own wallet lazy-create
  mechanism; the `partner-keys`/`outbound-config` "API-only, no UI" precedent) — full reasoning in the
  brief. One item flagged prominently rather than silently decided: internal-operator-only provisioning
  (not public self-serve signup) is the call closest to a genuine product-shape decision in this brief;
  proceeding on it per Arun's standing overnight authorization, not blocking, but surfacing for him to
  confirm or override in the morning. Also flagged, not touched: `app/api/webhooks/clerk/route.ts` is
  pure B2C-era `user.created` handling (retired `users` table, B2C welcome email, abandoned-onboarding
  timer) that still fires on every new Clerk sign-up — reusing it for partner-admin linking would
  conflate identity models; left as a separate cleanup candidate rather than folded into this brief.
  Zero open questions blocking BA dispatch. Next: dispatch BA Agent for the Requirement Document.
- **2026-07-15: B2B-06 revised to v2 — Arun overrode v1's Q1 directly, in his own words.** v1 (above)
  flagged internal-operator-only provisioning as "the one call in this brief closest to a genuine
  product-shape decision... flagging it prominently for Arun to confirm or override." Arun responded
  directly: *"someone from customer side will signup (capgemini or pluralsight)... our role will be to
  help them if they face any issues in signup or integration... those are sensitive and we should not
  see it hence it has to be self serve."* This is a direct override, not a refinement — v1's own
  supporting evidence for internal-operator-only was "Pluralsight and Capgemini are realistically
  negotiated deals, not self-serve"; Arun names those exact two companies as the self-serve example,
  meaning v1's central piece of reasoning was read backwards, not merely under-confirmed. CEO revised
  the brief same-day (`.claude/agents/clio/feature-briefs/B2B-06-partner-provisioning.md`, now v2)
  rather than writing a new brief, per the "revise in place, don't fork" instruction. Changes, each
  re-verified against live code today rather than carried over from v1's assumptions: **Q1** flips to
  self-serve via Clerk Organizations (confirmed genuinely new integration surface — `grep` across
  `lib/`/`app/` for any Organizations-API usage found only plain `clerkClient().users.*` calls, never
  `organizationMembership`/`Organizations`); **Q4** flips from "API-only, no UI" to three real UI
  surfaces — a minimal branded wrapper around Clerk's own prebuilt `<SignUp/>`/`<CreateOrganization/>`
  components (deliberately not a full designed marketing page, since the pivot's design system remains
  undefined and inventing one would violate the "flag as blocker, don't invent a visual direction"
  rule), a Configurator call site letting a partner self-generate their first API key (no new backend
  route needed — `POST /api/admin/partner-keys` already accepts any caller with a `partner_admin_users`
  row, confirmed today, not just Clio staff — the gap was UI-only), and a new self-serve outbound-config
  screen (reveal-once signing secret, Stripe's own webhook-secret UX pattern, plus a "Test connection"
  button) — folded into B2B-06 itself rather than a fast-follow, because it's the direct, load-bearing
  answer to Arun's stated reason self-serve is required at all ("those are sensitive and we should not
  see it"); **schema claim corrected** — v1 said "no migration needed," which was true only for v1's
  scope; v2 needs one new column, `partner_accounts.clerk_org_id TEXT UNIQUE`, to key a self-serve
  signup's Clerk Organization to its `partner_accounts` row. **New finding that surfaced only while
  re-verifying for this revision, not previously flagged**: read `docs/specs/B2B-04-requirement-document.md`
  §9 and `app/api/partner/v1/sessions/route.ts` directly (per the orchestrator's explicit instruction not
  to assume) — confirmed there is no funding/payment-method check anywhere in the real-session-dispatch
  path; §9 deliberately left this unblocked at the time, relying solely on manual
  `partner_accounts.status='suspended'` as mitigation, which was a reasonable gap only because every
  account used to be internal-operator-vetted before it could exist at all. Self-serve removes that
  vetting step, so v2 adds a small, explicitly-scoped cost-exposure guardrail: gates only
  fund-requiring (non-test-mode) session dispatch, never signup, never test-mode API/Configurator access
  — reuses the already-existing `partner_wallets.stripe_default_payment_method_id` field (already
  computed today for the `/dashboard/admin/clients` "payment method on file" column, confirmed via
  `app/api/admin/billing/clients/route.ts`) and the existing `account_suspended` error-envelope pattern
  already in `requirePartnerApiKey()` — no new schema, implementation lands in B2B-02/04-owned files
  (`lib/partner/auth.ts`, the sessions route) as a small named addition, not a rewrite. v1's two
  internal-operator routes (create/link/list) are preserved, not deleted, reframed as a SPOC
  manual-recovery fallback for exactly the support scenario Arun named today, per the "no delete without
  approval" standing rule. OAuth2 Client Credentials (Arun's "if they want to use JWT") named explicitly
  as a fast-follow — real, worth building, not required for v2 to be complete or safe to ship, not
  silently dropped. Q2 reframed (Clerk identity is now synchronous with account creation, unlike v1;
  `archetype` no longer required at signup, defaults `unspecified`, settable later from the admin table)
  and Q3 (wallet stays untouched, still lazy-creates) carried forward unchanged and re-confirmed rather
  than re-derived. Zero open questions blocking BA dispatch. Next: dispatch BA Agent for the Requirement
  Document — against v2, not v1.
- **2026-07-15**: CEO Feature Brief written for B2B-07 — Developer Portal (docs + playground), items 5
  and 6 of Arun's original 8-point vision, confirmed not built. See the B2B-07 row above for full
  detail. Headline finding: a same-day contradiction between `docs/brainstorm-partner-signup-integration.md`
  (Arun's direct instruction that OAuth2 Client Credentials is the day-one default auth mechanism —
  "We need this advanced login now itself. Let's not start with static API") and
  `B2B-06-partner-provisioning.md` v2's own written text (which still frames OAuth2 as an unscoped
  fast-follow, with the static API key as v2's actual in-scope mechanism). Both documents are dated
  2026-07-15; read together, the brainstorm conversation's decision was never written back into B2B-06
  v2's text, making v2 stale on this one specific point despite the shared date. Resolved by grounding
  in Arun's more recent, more explicit verbatim instruction rather than guessing or silently picking
  one — **B2B-06 needs a v3 revision (or a dedicated follow-on brief) to actually build the OAuth2
  client_id/secret + token endpoint mechanism**, named as a recommended near-term action, not resolved
  inside B2B-07 itself (issuing credentials is B2B-06's job). B2B-07 is scoped so it is not fully
  blocked in the meantime: the Documentation screen's non-auth content and the Playground's read-only
  usage/wallet flows can ship ahead of B2B-06 v3; only the sessions-endpoint Playground flow and the
  OAuth2-specific auth documentation are phased behind it. Also flagged as a hard pre-condition on the
  Playground's sessions-endpoint "Send" button specifically (not usage/wallet, which are pure reads):
  `dispatchMeetingBot()` (`lib/partner/session-init.ts`) does not currently branch on `test_mode`
  before calling Attendee, so a real Google Meet URL pasted into the Playground today would dispatch a
  real, billable bot — this is the same gap already named as an open item in the brainstorm doc, not
  new, but B2B-07 makes it an explicit, named launch blocker for that one flow rather than an implicit
  risk. Domain-config endpoints (`/api/admin/configurator/domain*`) verified via direct code read to be
  Clerk-session-gated (`requirePartnerAdmin()`), not part of the partner-API-key-authenticated surface
  — correctly excluded from the Documentation screen. No new npm dependency justified for either the
  JSON-body editor (plain `<textarea>`) or an OpenAPI-spec renderer (hand-rolled endpoint cards reusing
  the existing Configurator design system instead) — both would be unjustified additions for a
  4-endpoint surface. Zero open questions blocking BA dispatch, with the one named phasing dependency
  the BA should sequence around. Next: dispatch BA Agent for the Requirement Document.
- **2026-07-15 (later still)**: CEO Feature Brief written for the extraction-fix + internal glitch
  dashboard gap (Decisions 5–6 of `docs/brainstorm-partner-signup-integration.md`). **ID collision
  confirmed and resolved**: written first as `B2B-08` on a fresh read of this file and the
  feature-briefs directory showing no `B2B-08` anywhere; the sibling testing/metering brief (dispatched
  in parallel, by design) independently claimed the same ID roughly 3 seconds earlier. Per this brief's
  own stated tie-break rule, renamed to `B2B-09`
  (`.claude/agents/clio/feature-briefs/B2B-09-session-delivery-glitch-dashboard.md`) — `B2B-08` stays
  the testing/metering brief. Grounded in `docs/reference-vendor-api-integrations.md` §6–§8's exact
  `file:line` citations plus additional direct verification this brief performed itself: confirmed
  `hume_native_config_archives.session_id` is *also* hard-FK'd to `sessions(id)` (not covered by the
  reference doc), ruling out any archive-first transcript path for partner sessions — every partner
  extraction will always live-fetch from Hume directly. Confirmed `PartnerRenderClient.tsx`'s `onConnect`
  handler already receives the real Hume chat_id and discards it — the exact same signal
  `WalkthroughClient.tsx` already captures for Clio's own direct sessions via a proven, existing route
  pattern. Design direction: reuse `formatTranscriptLines()`/`callClaudeForExtraction()` (already
  exported, table-agnostic) and export the currently-private `fetchAllTranscriptEvents()` helper; do
  NOT generalize `extractActionItemsForSession()`/`getHumeSessionDetails()`/`session_action_items`
  with a source-table branch — write a new, smaller partner-specific orchestration function and a new
  `partner_session_insights` table instead, since the existing ones have real FK/Config-id structural
  assumptions that don't hold for `partner_sessions`. New `session.insights_ready` webhook event; one
  Anthropic call producing action items (full detail), glitches (full detail), psychology (keywords
  only) per Arun's exact corrected spec. Internal glitch dashboard recommended as a simple
  sortable/filterable table plus one aggregate summary view at `/dashboard/admin/glitches` (following
  the `/dashboard/admin/clients` Clerk-gated-any-signed-in-user precedent), not AI-assisted clustering,
  since no precedent exists yet for this kind of internal analytics screen to build clustering against.
  **Not cleared for BA dispatch — one genuine data-governance escalation, flagged directly to Arun, not
  resolved by the CEO unilaterally**: reliable webhook delivery (retry-with-backoff, matching every
  other event type) requires durably persisting the full-detail action-items/glitches payload until
  delivered, but `CORE_OBJECTIVES.md`'s Non-Negotiable Data Boundary (approved 2 days ago as part of
  B2B-01) and B2B-02's own `webhook_dispatch_log` design comment ("NEVER logged to this or any table")
  both restrict exactly this for session content. Three options written up in the brief (bounded-retention
  persistence in a new table / fully ephemeral with no retry / persist indefinitely), Option A
  (bounded retention, ~30-day purge window on full-detail fields, glitch type/partner/date retained
  longer for the dashboard) recommended but not decided — this revises the practical meaning of a
  data-governance statement Arun personally approved this week, for enterprise partners (Capgemini-scale)
  where the promise gets tested by a real security review. Everything else in the brief is fully
  specified and does not change shape under any of the three options.
- **2026-07-15: B2B-06 revised to v3 — same-day correction of a v2 document error on the auth mechanism.**
  The sibling CEO Agent working B2B-07 (row above) caught it while grounding that brief: v2's own
  "Auth/integration setup" text said OAuth2 Client Credentials was "explicitly named fast-follow, not
  v1/v2 scope," with the static API key as the actual in-scope v1 mechanism — but
  `docs/brainstorm-partner-signup-integration.md` Decision #2, captured in the same conversation, same
  day, records Arun's direct correction: *"We need this advanced login now itself. Let's not start with
  static API."* v2's document text was never updated to reflect that the conversation had already
  happened — a document error, not a new interpretation, and not a case of two conflicting instructions
  from Arun. v3 (`.claude/agents/clio/feature-briefs/B2B-06-partner-provisioning.md`) corrects this,
  scoped narrowly to the auth-mechanism section only — self-serve signup via Clerk Organizations, the
  outbound-config UI, the payment-guardrail scoping, and the internal-operator recovery routes are all
  unchanged from v2, re-confirmed not re-derived. OAuth2 Client Credentials is now scoped as the v1/
  day-one default: self-serve `client_id`/`client_secret` generation (reveal-once, same UX pattern as the
  outbound signing secret) as a partner's first credential; new token endpoint
  `POST /api/partner/v1/oauth/token` (matches the existing `/api/partner/v1/*` route convention, mirrors
  the exact shape Clio's own upstream voice vendor already uses for this grant type — Hume's live
  `oauth2-cc/token` endpoint, `app/api/hume-token/route.ts:18-25`); `requirePartnerApiKey()` (or its
  replacement) extended to accept a short-lived OAuth2 token alongside the existing static key, with zero
  changes needed to the `sessions`/`usage`/`wallet` route call sites; one new table,
  `partner_oauth_clients` — deliberately not an extension of `partner_api_keys`, since `client_id` is a
  standalone identifier (not a truncated prefix of a secret, unlike `key_prefix`) and overloading one
  table with both shapes plus a second orthogonal `credential_type` dimension on top of the existing
  `mode` enum would produce a confusing 2x2 state matrix for no real reuse benefit. **Static-key decision,
  made explicitly rather than left implicit**: preserved, not deleted, per the standing "no delete without
  approval" rule and because `partner_api_keys`/`requirePartnerApiKey()`/every existing
  `/api/partner/v1/*` route already depend on it and already work correctly — demoted to secondary,
  reachable only through the unchanged internal-operator route (`POST /api/admin/partner-keys`) for SPOC
  support/recovery, no longer the self-serve partner's default or first-generated credential. v2's planned
  self-serve static-key-generation Configurator call site is not built in v3; the OAuth2 client-generation
  flow replaces it. New Q5 added to Questions for BA, resolving the OAuth2/static-key scoping explicitly;
  Q1-Q4 unchanged and not reopened. Zero open questions. Next: dispatch BA Agent for the Requirement
  Document (now against v3, not v2).

- **2026-07-16**: B2B-10 (Attendee webhook — partner session support) shipped full chain. CEO brief
  written per Arun's direct correction that the Attendee webhook is Clio's general-purpose
  bot/meeting-mechanics event handler, already proven for B2C, that needed *extending* for partner
  sessions rather than a new build. Grounding found the correlation mechanism already existed,
  undocumented: `dispatchMeetingBot()` (`lib/partner/session-init.ts`) passes `partner_sessions.id`
  into the Attendee provider's `userId` metadata slot, so every partner-dispatched bot's webhook
  events already carry the right correlation key with zero new dispatch-time tagging — confirmed by
  direct code read, not assumed. Design: additive-only fallback in `handleEvent()` (B2C
  `walkthrough_state` lookup unchanged, tried first; `partner_sessions` lookup only on miss);
  `joined_recording` confirmatory-log-only; `ended`/`fatal_error` as an idempotent fallback safety
  net behind the client-side `endSessionOnce()` → `handleSessionEnd()` path (checks
  `partner_sessions.status` before acting, so whichever path lands first wins, no double-billing);
  `transcript.update` a deliberate no-op per the Non-Negotiable Data Boundary (mirrors B2B-09's own
  boundary, does not reopen it); `participant_events.join_leave` a deliberate no-op, correlated and
  logged only — the one real product-shape question (should partner sessions get a join greeting,
  in what voice, delivered how) named explicitly and deferred to Arun rather than guessed or built
  speculatively, since no delivery mechanism to a live partner Hume session exists today either.
  BA/CEO review caught one real factual error in the brief's own grounding — `endSessionOnce()` was
  described as having three call sites; direct code read showed only two (`onDisconnect` only sets
  UI status, never calls it) — corrected with no design change needed, since the fallback only
  checks `partner_sessions.status`, not which client call site fired. Zero schema changes —
  `partner_sessions` (migration `071`) already had every column needed. `handleSessionEnd()` gained
  one optional, backward-compatible `targetStatus` parameter (defaults `'completed'`) so
  `fatal_error` can land as `'failed'` without touching its one existing call site. 9 new
  integration tests, `tsc --noEmit` clean (independently re-verified), full suite green apart from
  the same 1 pre-existing unrelated failure every other B2B brief this session has noted. Not yet
  committed.
- **2026-07-18 (later)**: All four B2B-21..24 CEO briefs carried through BA spec → CEO review → Dev in sequence (B2B-21 immediately unblocked; B2B-23 immediately unblocked once C5/Domain and the bug-chart-scope decisions came back from Arun; B2B-22 unblocked once B2B-21's BA spec defined the role model; B2B-24 built directly on B2B-23's landed `GO_LIVE_REQUIRED_STEPS` change per the sequencing note both CEO reviews flagged). **B2B-21, B2B-23, B2B-24 are fully built, `tsc`/test-clean, not yet committed.** B2B-22's spec is CEO-approved (v1.1, after one real revision cycle — see its row) but Dev is held until B2B-21 actually merges to `main`, since B2B-22's code imports B2B-21's new auth helpers directly. **One unplanned finding needs Arun/CEO awareness**: the B2B-21 dev agent found a fifth route with the same zero-auth defect the spec's own route table already caught twice — `app/api/admin/billing/invoice/route.ts` (enterprise Stripe invoicing) — not named in the approved spec's §7 table. Gated it under the same P0 reasoning as the two spec-named routes, documented inline, but this is a deviation from the literal approved spec (an omission in the spec's own table, not an invented fix) and wasn't independently re-verified by a CEO re-review pass the way everything else in this arc was.
- **2026-07-18 (later)**: The `ece1385` production build failed on Vercel (`errorCode: lint_or_type_error`) — a real bug that every dev agent's local verification missed. Root cause: `ConfiguratorSurface.tsx` and `DashboardPanel.tsx` (both `'use client'`) had real value-imports (`VISIBLE_SECTIONS`, `GO_LIVE_REQUIRED_STEPS`) from `configurator-status.ts`/`wizard.ts`, both of which import `lib/supabase.ts` for `next/headers` — a server-only module can't be pulled into a client component's bundle. `npx tsc --noEmit` (what every B2B-21/23/24 dev agent ran) does **not** catch this class of error, since TypeScript type-checking doesn't enforce the React Server Components client/server boundary — only the real `npm run build` does. **Process gap, not just a code bug**: no dev agent this session ran the actual Next.js build before reporting "tsc clean" as sufficient verification. Fixed by extracting the client-safe constants (`VISIBLE_SECTIONS`, `GO_LIVE_REQUIRED_STEPS`, `ConfiguratorSection`/`ConfiguratorStatus` types) into a new zero-server-dependency module, `lib/partner/configurator-sections.ts`; `wizard.ts` and `configurator-status.ts` now import from it and re-export for their existing server-side callers, and the three affected client components import directly from the new module. Verified with the actual `npm run build` this time (clean, full route manifest generated) before committing/pushing as `c7783e0`. **Standing lesson for future dev-agent dispatches in this project**: verification must include `npm run build`, not just `tsc --noEmit` — the two check different things (type correctness vs. RSC bundling boundaries) and only the build catches server-only-import leaks into client components.
- **2026-07-18**: Four CEO Feature Briefs dispatched in parallel per Arun's single large scoping message (super-admin/sales-partner roles; Configurator scope reduction + responsive fix; partner-facing Known Bugs screen; Configurator Dashboard/Overview screen). All four independently read the feature-briefs directory as topping out at `B2B-20` and claimed `B2B-21` simultaneously — a 4-way ID collision, each agent's own self-resolution attempt disagreed with the others'. **Orchestrator resolved by direct file `mtime` inspection** (`stat -f "%m %N"`, not trusting any agent's self-report): `B2B-21` = Internal Admin Identity (filed first, kept its ID) → `B2B-22` = Partner-Facing Known Bugs (renamed from its self-claimed `B2B-21`) → `B2B-23` = Configurator Milestone Scope Reduction (renamed from its self-claimed `B2B-22`) → `B2B-24` = Configurator Dashboard/Overview (renamed from its self-claimed `B2B-22`, having incorrectly believed itself 2nd-filed when mtime shows it was actually last). Files renamed, headers and every internal cross-reference between the four briefs corrected to the ratified IDs. See the four new Live Status rows above for content summaries. Two decisions now need Arun directly: (1) B2B-22's aggregate bug-count chart — count all bugs or only ever-visible ones (CEO recommends only-visible); (2) B2B-23's C5/Domain resolution (A/B/C) which also determines the final Go-Live required set both B2B-23 and B2B-24 depend on.

## Backlog — deferred, not forgotten

- **Attendee webhook signature hard-enforcement.** `app/api/attendee/webhook/route.ts` was found (2026-07-14/15
  overnight audit) with signature verification computed but never enforced — any POST claiming to be from
  Attendee was accepted. Fixed 2026-07-15 with Attendee's real documented signing algorithm (canonical-JSON
  HMAC-SHA256, per `https://attendee.dev/blog/webhooks-implementation-guide`), but deployed in **soft-verify
  mode**: mismatches are logged loudly but the event still processes, since the new algorithm has never been
  confirmed against real Attendee traffic (none in the observable log window). Arun's instruction: sort this
  out separately later, don't let it block the reuse work below. **Next step when picked up**: watch Vercel
  logs for `[attendee/webhook] sig_match:` on the next real session, confirm `true`, then flip the `if
  (!match)` branch to `return 401` instead of falling through.

- **Reconstruct lost B2B-06/07/08/09 governance documents.** A concurrent-agent `git stash` collision
  during the parallel B2B-06/07/08/09 build spree (2026-07-15) wiped, and nobody caught: all 4 CEO
  Feature Brief files (`.claude/agents/clio/feature-briefs/B2B-0[6-9]-*.md`), and 3 of 4 Requirement
  Documents (`docs/specs/B2B-07-requirement-document.md`, `B2B-08-requirement-document.md`,
  `B2B-09-requirement-document.md` — B2B-06's survived because it happened to be recreated by its own
  build agent minutes before the loss was discovered). **Not lost**: `architecture.md` §§15-18 (the
  full technical spec for all four briefs, fully intact), both migration files (077, 078 — reconstructed
  2026-07-15 from the exact SQL used in their original `apply_migration` calls, verified against the
  live Supabase migration list), and all shipped application code (verified via `tsc`/test suite before
  and after reconstruction). Arun's decision (2026-07-15): commit the code now (Option A), reconstruct
  the missing governance docs as a follow-up, not blocking. **When picked up**: `architecture.md` §§15-18
  has the full technical detail to reconstruct the Requirement Documents' "how"; the CEO Feature Briefs'
  "why" (user stories, original scoping reasoning, resolved escalations) needs to be rebuilt from this
  file's own Changelog entries for B2B-06 through B2B-09 plus Orchestrator memory of the review cycles —
  both are still available, this doesn't need re-deriving from scratch.
  - **2026-07-15: B2B-08's CEO Feature Brief reconstructed** — `.claude/agents/clio/feature-briefs/
    B2B-08-testing-metering.md` rebuilt from `architecture.md` §15, this file's B2B-08/B2B-09 rows and
    2026-07-15 Changelog entries, and the shipped code's own comments (`inngest/partner-trial-cutoff.ts`,
    `lib/stripe.ts`'s `createTestBlockCheckoutSession`, the sessions route's test-mode gate). Marked
    RECONSTRUCTED at the top with sourcing. `docs/brainstorm-partner-signup-integration.md` itself was
    confirmed never committed to this repo (no git history under that path) — the two Arun quotes it
    contained ("20 minutes of free bot usage... pay for 2 hours every time," "That cost is on us. We
    will send real bot.") survive verbatim only via this file's own Changelog and were reproduced from
    there, not re-derived. Still outstanding: B2B-07's CEO brief + Requirement Document, B2B-09's CEO
    brief, and B2B-08's own Requirement Document (`docs/specs/B2B-08-requirement-document.md`) — none
    of the shipped B2B-08 code or schema changes as a result of this reconstruction.
  - **2026-07-15: B2B-06/08/09 CEO Feature Briefs reconstructed**, all three cross-verified against
    `architecture.md` and shipped code before being trusted. Also confirmed, independently and via two
    routes (a fresh `git log --all -- <path>` on both filenames, and a reconstructing agent's own
    unprompted flag): `docs/brainstorm-partner-signup-integration.md` and
    `docs/reference-vendor-api-integrations.md` — both cited throughout this file's own Changelog and
    in the prior conversation summary as committed reference docs — **were never actually committed to
    this repo. No git history exists for either path, under any commit, on any branch.** This corrects
    the prior session summary, which stated both were created. Every direct quote attributed to them in
    this file's Changelog survives correctly (the two Arun quotes on testing/OAuth2 are verbatim and
    reliable), but the documents themselves do not exist and were not silently fabricated during
    reconstruction — each reconstruction agent sourced only from this file's own Changelog text instead
    and flagged the gap explicitly rather than inventing surrounding context.
  - **2026-07-15 — CONFIRMED GAP, more serious than a lost document: B2B-07's UI was never actually
    built**, despite this file's B2B-07 row and the prior session's own task tracker both having
    recorded a "Build B2B-07 (Developer Portal)" step as complete, and despite commit `2d3b3d0`'s own
    message claiming "B2B-07 developer portal" shipped alongside B2B-06/08/09. Verified directly, not
    assumed: `ls app/dashboard/configurator/developer/` → no such directory; `git show 2d3b3d0 --stat`
    lists all 43 changed files in that commit and none are under that path; `git log --all
    --diff-filter=A --name-only | grep -i playgroundclient` → zero hits on any branch, ever; checked
    every `.claude/worktrees/agent-*` worktree individually for the directory → not present in any of
    them either. The technical spec for these screens is fully intact in `architecture.md` §17 (file
    layout, the complete `content.ts` `ENDPOINTS` array, `PlaygroundClient.tsx`'s `handleSend()` logic),
    so nothing about *what was decided* is lost — but `content.ts`, `DeveloperDocsClient.tsx`,
    `PlaygroundClient.tsx`, and the route's `page.tsx` were never written to disk, or were written and
    lost to the same stash collision without ever being caught (unlike the B2B-09 Inngest casualty and
    the B2B-06 spec casualty, both of which were independently diffed and confirmed reconstructed at
    the time). Corrected: the B2B-07 Live Status row below now reflects "spec complete, UI not built"
    instead of implying the build finished. **Action needed**: B2B-07's Documentation + Playground
    screens need an actual build pass, not just a document reconstruction — tracked as a fresh build
    task, not folded into the reconstruction-doc backlog item above.

- **2026-07-16 — CONFIRMED SYSTEMIC PATTERN: three of the four B2B-06/07/08/09 briefs shipped with
  missing pieces the commit `2d3b3d0` message and this file both claimed were complete.** After the
  B2B-07 gap above was found, an independent verification pass was run against B2B-06/08/09's actual
  shipped code (not just architecture.md prose) specifically to check for the same class of gap. Full
  findings, most severe first:
  - **B2B-09 (most severe)**: `app/api/partner/render/session-chat-id/route.ts` — the only write path
    for `partner_sessions.hume_chat_id` — was never created. `PartnerRenderClient.tsx`'s `onConnect`
    handler calls it and silently swallows the failure via `.catch()`, so this has never once been
    caught by any error monitoring. Net effect: `partner_sessions.hume_chat_id` stays NULL forever for
    every real session, so the Hume webhook's fallback lookup (confirmed real and correct) never
    matches anything, and the extraction backstop sweep's own eligibility query explicitly filters
    `.not('hume_chat_id', 'is', null)` so it never picks up a partner session either. **The commit's own
    headline claim — "fixes the dead transcript-extraction pipeline for partner sessions" — is false in
    production.** It now fails one step later (a missing join key) than before this brief, instead of
    failing for the original reason (no fallback logic existed at all). Separately, `app/api/admin/glitches/`
    (both `route.ts` and `summary/route.ts`) was never created at all — `GlitchDashboardClient.tsx` (386
    lines, genuinely real, not a stub) calls both and 404s every time; the dashboard has never once
    successfully loaded data.
  - **B2B-08**: `lib/stripe.ts`'s `createTestBlockCheckoutSession()` is real and correct, and the Stripe
    webhook's crediting branch is real and correct, but `POST /api/admin/billing/test-block` — the route
    architecture.md §15.2 requires to actually call the checkout-session function — was never created,
    and no UI anywhere references "test block." The mechanism is backend-complete but has no entry
    point; a partner cannot buy a test block today. Lower severity than B2B-09's gap (isolated, one
    route + minimal UI to close) but the same root cause.
  - **B2B-06**: fully verified, no gaps found. Every claimed deliverable (self-serve signup, OAuth2
    Client Credentials, the outbound-config UI, the funding guardrail, the dual-FK schema fix) is real,
    substantive, and correctly wired.
  - **Root cause, consistent across all three real gaps found (B2B-07's missing UI, B2B-08's missing
    route, B2B-09's two missing routes)**: every one of them is a route or screen that a *different*
    concurrently-running build agent's client code (`PartnerRenderClient.tsx`, `GlitchDashboardClient.tsx`,
    the Playground) depended on, but that nobody actually finished writing before the git-stash
    collision — and because `tsc --noEmit` and the existing test suite don't exercise these specific
    call sites, "clean build, tests pass" gave false confidence at the time. This is a genuine
    verification gap in how "done" was previously assessed for this build spree, not a one-off fluke.
  - **Action needed**, tracked as fresh build/fix tasks, not folded into the reconstruction-doc backlog
    item above: (1) `app/api/partner/render/session-chat-id/route.ts` — write `hume_chat_id` onto
    `partner_sessions` at session start, unblocking B2B-09's entire extraction pipeline; (2)
    `app/api/admin/glitches/route.ts` + `summary/route.ts` — back the existing, already-correct
    dashboard UI; (3) `POST /api/admin/billing/test-block` — wire the existing, already-correct
    checkout-session function to a real endpoint and a minimal admin UI trigger; (4) B2B-07's
    Documentation + Playground screens, per the entry above.

- **2026-07-16 — Homepage redesign for the B2B pivot + competitor research.** Arun's exact instruction,
  logged verbatim: the marketing homepage needs a full redesign aligned with the new B2B/B2B2C goals
  (the current one is B2C-era, per this file's earlier note that the untouched B2C surface was never
  revisited). Scope, all explicitly gated as **research/documentation only — no build without Arun's
  separate approval**:
  1. Dispatch the CEO Agent to research comparable competitor products/websites, identify what they're
     missing or doing poorly, and evaluate which of those gaps Clio should close on its own homepage.
  2. Homepage should include a live interactive demo — something that lets a visitor see the
     visualization + voice-response experience work in real time, not just static marketing copy.
  3. Output of this phase: a CEO Feature Brief (or research doc) and, once Arun reviews it, a BA
     Requirement Document — following the normal governance chain, but **frozen at the brief/spec
     stage** until Arun explicitly says to proceed to build.
  **Explicitly NOT started yet** — Arun's instruction was to log this now and wait for a separate "go"
  signal before dispatching any research agent. Do not begin the competitor research until that signal
  arrives, even though the CEO→BA chain would otherwise be the default next step for a new backlog item.
  **Also per Arun's instruction**: he is now actively testing the live B2B-06 through B2B-11 build on
  `hello-clio.com` (now confirmed fully online, DNS/SSL resolved 2026-07-16) — his testing findings and
  feedback should be captured here as he reports them, in real time, not batched.

## Live QA Findings — 2026-07-16 (Arun testing hello-clio.com directly)

Running log, captured as reported. **Noted only, not fixed** — Arun is testing in a batch and will say
when to start acting on these; do not jump to fixing individually unless he says so.

1. **"Get Started" button routes to the wrong place.** Homepage's Get Started CTA should take the
   visitor to partner signup (`/partner-signup`), not wherever it currently points. Likely a leftover
   from the pre-pivot B2C homepage (which pointed Get Started at the old consumer onboarding flow).

2. **Clerk's `<CreateOrganization />` blocks partner signup entirely — "Organizations feature required"
   dialog appears instead of the real signup UI.** Not a code bug — the Organizations feature is not
   yet enabled on the Clerk instance/dashboard for this project, and B2B-06's entire self-serve signup
   flow is built on Clerk Organizations (one org per partner, admin users must belong to it). Blocks
   every downstream step (onboarding wizard, Configurator, everything) until fixed. Fix is a Clerk
   Dashboard setting, not a file change — steps to be given to Arun when he asks for them (he's
   deferring remediation until his testing pass is done); when asked, the answer is: Clerk Dashboard →
   enable Organizations → choose "Membership required" (not "Membership optional" — matches B2B-06's
   design that every partner-admin belongs to exactly one organization, no personal/orgless accounts).

3. **Payment step during onboarding shows wallet-funding choices, not subscription plan tiers.**
   Confirmed by direct code read (`WizardClient.tsx`'s `PaymentStep`): today's UI is two cards —
   "Pay as you go" (one-time top-up, hardcoded `$100`, hits `/api/admin/billing/checkout`) and "Set a
   monthly minimum" (auto-recharge subscription, hardcoded `$100`, hits `/api/admin/billing/subscription`).
   No plan names, no feature comparison, no monthly/annual toggle. Arun said this is OK as a mechanism,
   but the hardcoded `$100` amounts need updating once he configures real prices in Stripe (his own
   action — noted, not something I fix in code, since there are no Stripe Price IDs to update yet, only
   ad-hoc `price_data` line items generated per-request in `lib/stripe.ts`).
   - **Arun asked for a recommendation on pricing display UX** (specific prices vs. a list/selector) and
     stated a new requirement: real monthly/annual recurring plans, plus one-time top-ups, both needed —
     not just the current pure-wallet mechanism. **Important finding to flag**: the live billing model
     (B2B-04, migration 075) has **no concept of discrete recurring plan tiers at all** — `partner_wallets`
     is purely prepaid-balance + metered usage; the only "tier" column (`self_serve`/`mid_market`/
     `enterprise`) is explicitly commit-size/support-routing only, "no feature gating by tier" per its own
     schema comment. B2C-era plan-tier UI/constants ("Starter"/"Pro"/"Executive") still exist in
     `app/plan/PlanClient.tsx`, `app/(marketing)/pricing/page.tsx`, `app/checkout/page.tsx` but are fully
     disconnected from the wizard and the wallet schema — dead B2C surface, not reusable as-is. **This
     means "add monthly/yearly plans + topups" is new scope, not a UI tweak** — it needs an actual
     recurring-subscription-tier mechanism added to the billing model, not just a redesigned payment step.
     Recommendation on display UX to be given once this is scoped as a proper brief — noted here as owed,
     not answered yet, since it depends on resolving the tier-mechanism question first.
   - **Closed 2026-07-16**: scoped as `B2B-12` (see Feature Briefs table above) — CEO brief written with
     the tier-mechanism question reasoned through explicitly (new `partner_wallets` columns + a code-level
     plan catalog, not a DB table) and the display-UX recommendation given (specific-price compact cards
     matching the Configurator's own idiom, not a selector). Awaiting BA dispatch, not yet built.

4. **`/dashboard` (root, no `/configurator`) is the old B2C dashboard, still fully live and apparently
   still the default post-sign-in landing page.** Confirmed by direct code read: `app/dashboard/page.tsx`
   queries B2C tables directly (individual `users` row by Clerk `userId`, `delivery_log`, per-user
   `sessions`), renders B2C copy ("Your AI learning command center") — this was supposed to have been
   retired per this file's own "Project Structure" section ("the old B2C file tree... was removed as
   stale — do not rebuild it"), but it was never actually deleted and is still reachable. Very likely
   cause: `NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL`/`AFTER_SIGN_UP_URL` env vars on Vercel are still set to
   their original B2C values (`/dashboard`, `/onboarding`) rather than updated for the B2B Configurator
   flow — not independently confirmed (env var read access wasn't available when this was found), needs
   checking directly in Vercel's dashboard. Workaround given to Arun: navigate directly to
   `/dashboard/configurator`. **Real fix, not yet done**: (a) update the Clerk redirect env vars to point
   at `/dashboard/configurator` instead of `/dashboard`, (b) decide whether to actually delete the old
   B2C dashboard route/component tree now that it's confirmed still live and causing real confusion, or
   just gate it off — ties directly into task #44 ("Discuss untouched B2C surface"), which this finding
   makes considerably more urgent than a cleanup nice-to-have.
   - **Decision confirmed by Arun (2026-07-16): remove it, don't just gate it.** "Plan," "session," and
     the rest of the old B2C dashboard surface are not needed anymore under the B2B pivot goals — delete,
     not redirect-around. Closes task #44's open question. **Noted only, not yet built** — per Arun's
     standing instruction this whole testing pass, this is added to the list, not actioned yet. Scope
     when picked up: `app/dashboard/page.tsx` + `DashboardClient`/`DashboardShell`, `app/plan/`,
     `app/dashboard/plan/`, `app/(marketing)/pricing/page.tsx`, `app/checkout/page.tsx` (the B2C plan-tier
     UI flagged as dead in finding #3 above), and the Clerk redirect env vars pointing at any of this —
     full inventory of what else references these routes/tables should be swept before deleting, not
     assumed from this list alone.

{
  "_readme": "Persistent action-items tracker for Clio. One row per tracked issue/decision. 'status' is the current state; 'comment' holds ONLY the latest status/action note (no history needed — full history lives in the conversation transcript). Update this file the instant any item's status changes — do not batch. Add new items the moment they're raised, even if not yet investigated.",
  "items": [
    {
      "id": "double-charge-risk",
      "category": "Billing & session time",
      "item": "Double-charge risk",
      "description": "A call ending manually and automatically at the same time could deduct minutes twice.",
      "status": "fixed, deployed",
      "comment": "Closed via cycle-scoped billing math plus billing off Hume's own call duration as source of truth. Live in production."
    },
    {
      "id": "session-length-field-overloaded",
      "category": "Billing & session time",
      "item": "Session length field is overloaded",
      "description": "One field stored both the plan and the actual usage, overwriting the plan every time a call ends.",
      "status": "fixed",
      "comment": "Split into a separate planned-length field and actual-usage field. Migration applied. Historical sessions left as unknown, only new sessions affected."
    },
    {
      "id": "finished-session-rejoinable",
      "category": "Billing & session time",
      "item": "A finished session can be rejoined",
      "description": "Nothing stopped re-entering a call that already ended — the root enabler of the field-overload issue above.",
      "status": "fixed",
      "comment": "Added a check that blocks rejoining a session once it's marked completed."
    },
    {
      "id": "test-call-ended-2-41",
      "category": "Billing & session time",
      "item": "Test call ended after only ~2:41",
      "description": "A call ended from the user side, not by Clio deciding the session was done.",
      "status": "backlog — discuss later",
      "comment": "Moved to backlog per Arun's direction 2026-07-06 morning. Not actively worked until revisited."
    },
    {
      "id": "missing-background-material",
      "category": "Content & how Clio speaks",
      "item": "Missing background material",
      "description": "Every session showed a placeholder instead of real subject background.",
      "status": "fixed",
      "comment": "Real background content now generated and wired in before every session."
    },
    {
      "id": "character-limit-warning",
      "category": "Content & how Clio speaks",
      "item": "Character-limit warning in Hume's UI",
      "description": "A character-limit warning appeared in Hume's dashboard.",
      "status": "resolved, safeguard added",
      "comment": "Confirmed real limit, but only affects a voice-tone layer we don't rely on. Added a guardrail to stay under it regardless."
    },
    {
      "id": "no-greeting-opening",
      "category": "Content & how Clio speaks",
      "item": "No proper greeting at session open",
      "description": "Clio doesn't open the session herself — she reacts to the participant speaking first instead of greeting them by name.",
      "status": "fixed, deployed, confirmed working",
      "comment": "Confirmed by Arun on a real live call 2026-07-07: Clio now opens the conversation herself and greets him by name. Root cause was a leftover technical message from the old voice system blocking her own greeting; fixed by skipping it specifically for the new system, with the signed-up user's first name threaded in. Closed."
    },
    {
      "id": "graceful-ending-wrapup",
      "category": "Content & how Clio speaks",
      "item": "Abrupt call ending, no graceful wrap-up",
      "description": "Calls ended abruptly with no summary or goodbye.",
      "status": "fixed",
      "comment": "Added a live wrap-up nudge near the end plus a permanent closing-behavior rule (summary then goodbye). Deployed."
    },
    {
      "id": "web-search-missing",
      "category": "Voice setup & configuration",
      "item": "Web search tool sometimes missing",
      "description": "The web search capability worked sometimes and silently disappeared other times.",
      "status": "fixed",
      "comment": "Root cause was a code bug reading tools dynamically from the wrong shape; fixed and confirmed present on a real live call."
    },
    {
      "id": "third-capability-missing",
      "category": "Voice setup & configuration",
      "item": "A third capability is silently missing",
      "description": "A third custom tool (likely a way to gracefully defer a question) exists on the account but never gets carried into new sessions.",
      "status": "fixed, deployed — needs a real call to fully confirm",
      "comment": "Shipped 2026-07-06 per DEFER-QUESTION-01. The Hume-native prompt now tells Clio to verbally acknowledge deferring a question with no tool call, using natural phrasing anchored on 'next time'/'next session'/'cover that properly'. The old ElevenLabs-path prompt was also updated similarly but is a separate, untouched path. inngest/session-quality-evaluator.ts has two detectors, gated by session.hume_native_enabled. Both write to the same existing sessions.deferred_questions column, same existing UI. 7 new unit tests passing, full suite 420/422 (2 pre-existing unrelated failures untouched). Live in production — needs one real live Hume-native session where Clio actually defers something to fully confirm end-to-end."
    },
    {
      "id": "settings-reported-oddly",
      "category": "Voice setup & configuration",
      "item": "Some settings reported back oddly",
      "description": "One data point suggested a setting mismatch, but it came from an error response, not a real call.",
      "status": "P1 — backlog, after Arun finishes testing",
      "comment": "Not yet logged/investigated with a clear record. Needs a fresh check against a real call before trusting it. Marked P1 by Arun 2026-07-06 — investigate/build once he's done with his current testing pass."
    },
    {
      "id": "overnight-backup-config-tool",
      "category": "Backup & diagnostics",
      "item": "Overnight backup / config-checking tool",
      "description": "The nightly job that archives and cleans up session configs was broken because a database table it needed was never created.",
      "status": "deployed to production, awaiting first real overnight run",
      "comment": "Confirmed live in production (checked 2026-07-06 morning — current deployed build includes this fix and is several commits further ahead). Just hasn't had its first real overnight run since going live to fully confirm end to end."
    },
    {
      "id": "quality-check-failed-call",
      "category": "Backup & diagnostics",
      "item": "Automated quality check flagged a call",
      "description": "Our own quality system flagged that no proper coaching speech was detected on one test call.",
      "status": "P1 — backlog, after Arun finishes testing",
      "comment": "Still open. Needs a deeper look on its own. Marked P1 by Arun 2026-07-06 — investigate/build once he's done with his current testing pass."
    },
    {
      "id": "unknown-session-names",
      "category": "Older, unrelated",
      "item": "\"Unknown session\" names in history",
      "description": "A separate, pre-existing bug quietly stripping session names out of the learner's history.",
      "status": "P1 — backlog, after Arun finishes testing",
      "comment": "Confirmed unrelated to recent changes. Also confirmed it leaks into what Clio reads before a real call, not just a display issue. Marked P1 by Arun 2026-07-06 — investigate/build once he's done with his current testing pass."
    },
    {
      "id": "hume-duration-billing-timing",
      "category": "Fixed overnight (last test call)",
      "item": "Verified-minute billing silently falling back",
      "description": "Billing was asking Hume for the real call length too soon, before Hume finished recording it, and silently falling back to a less accurate method.",
      "status": "fixed, deployed",
      "comment": "Now waits, and tries once more, before falling back. Covered by a new automated test (6 scenarios, all passing). Live in production. Superseded/complemented by hume-ground-truth-webhook below, which removes the wait entirely when it fires."
    },
    {
      "id": "screen-overview-summary-missing",
      "category": "Fixed overnight (last test call)",
      "item": "Screen stuck / missing Overview and Summary screens",
      "description": "The shared screen got stuck on the opening title card for an entire call. Root cause: the Overview screen never existed in the live call's screen list at all, so every topic displayed one slot ahead of what was being said.",
      "status": "fixed, deployed",
      "comment": "Added real Overview and Summary screens into every place that builds a session's screen list (3 places found and aligned). Skipped topics correctly excluded from the summary. Type-checks clean, no regressions found. Live in production — however, a separate live-conductor shadowing bug meant this screen wasn't actually reaching users until that bug was found and fixed on 2026-07-06; it was not previously tested/confirmed working as this comment had claimed."
    },
    {
      "id": "contradictory-closing-instructions",
      "category": "Fixed overnight (last test call)",
      "item": "Contradictory closing instructions",
      "description": "One instruction told Clio to ask \"any more questions?\" and wait; another said to skip that and call a tool that no longer exists.",
      "status": "fixed",
      "comment": "Removed the stale instruction so only the correct, newer closing behavior remains."
    },
    {
      "id": "script-sounds-written",
      "category": "Fixed overnight (last test call)",
      "item": "Script sounded written, not spoken",
      "description": "The material Clio was told to say read like a dense written article rather than natural spoken coaching.",
      "status": "fixed (scoped)",
      "comment": "Added explicit \"sound spoken, not written\" guidance to the content generator. Scoped instruction addition, not a full pipeline rewrite."
    },
    {
      "id": "repeated-checkin-phrase",
      "category": "Fixed overnight (last test call)",
      "item": "Same check-in phrase repeated every section",
      "description": "The fallback verification question repeated verbatim every section.",
      "status": "fixed",
      "comment": "Now rotates through 4 natural variants instead of repeating the same line."
    },
    {
      "id": "retake-completed-session",
      "category": "Session lifecycle",
      "item": "Retake a completed session",
      "description": "A completed session can't be rejoined (by design, to prevent double-charging). Arun wants a way for the user to take that session's material again as a brand-new attempt — new session, new call, new billing, same topic.",
      "status": "removed — Arun decided this was unnecessary complexity, superseded by simply removing the restart block instead",
      "comment": "2026-07-06: Arun reviewed this and the underlying restart block together and decided both should go. The 'Retake this session' button/modal/API route (app/api/sessions/[id]/retake/route.ts) were deleted; the retaken_from_session_id column is left in place, unused, no down-migration. See REVERT-01 below and docs/specs/REVERT-01-remove-restart-block-and-retake.md. Historical spec/brief kept at .claude/agents/clio/feature-briefs/RETAKE-01-*.md and retake-completed-session.md for record."
    },
    {
      "id": "revert-01-restart-block-removed",
      "category": "Session lifecycle",
      "item": "Removed the hard block on restarting a completed session",
      "description": "SESSION-DURATION-01 added a guard that flatly blocked ever restarting a session once status === 'completed', on top of the actual root-cause fix (a calculation-scoping correction in computeBilledMinutes()). Arun decided the guard was an unnecessary safety net, not a requirement — entering a meeting URL and clicking the normal launch flow on a completed session should just work again.",
      "status": "done, deployed",
      "comment": "2026-07-06: removed the 409 SESSION_ALREADY_COMPLETED guard from app/api/sessions/[id]/start/route.ts. A completed session now goes through the identical start path as a scheduled one (status -> active, audit token, bot_joined event); plan-approval and minutes-balance checks unchanged. The real root-cause fix in lib/session-billing.ts (computeBilledMinutes()) was left completely untouched — still load-bearing for the Hume-native fallback path. This also let RETAKE-01 be removed entirely (see above). Live in production — ready to test."
    },
    {
      "id": "hume-ground-truth-webhook",
      "category": "Billing & session time",
      "item": "Instant, exact billing via Hume's own end-of-call webhook",
      "description": "Today, billing waits ~3-7 seconds after a call ends and asks Hume for the call length (with one retry if Hume hasn't finalized it yet), only falling back to our own estimate if that fails. This adds a faster, more direct path: Hume can proactively tell us the second a call ends, so we don't have to wait and ask.",
      "status": "deployed, migration applied, secret set — needs 1 live test to confirm the webhook actually registers",
      "comment": "2026-07-06: CEO-agent code review completed. Confirmed correct: signature verified before trusting any data, stale-webhook-row protection reuses the proven cycle-scoping logic, and the existing wait-and-ask billing path is completely unchanged if this fast path doesn't fire — cannot make billing worse. Migration 062 applied, HUME_WEBHOOK_SECRET set in Vercel Production, code deployed. ONE OPEN ITEM: the exact field/shape for registering the webhook with Hume is a well-reasoned best guess, not yet round-trip confirmed against Hume's real API. Confirm on the next real test call by checking whether a 'hume_webhook_chat_ended' audit row appears for that session — if not, billing still works exactly as before, just without the speed-up yet."
    },
    {
      "id": "recall-webhook-bypasses-billing",
      "category": "Billing & session time",
      "item": "A call ending via the meeting-recording tool's own detection skips billing entirely",
      "description": "When the meeting-recording tool notices everyone left the call and marks the session done on its own, it never goes through the billing process at all — no minutes counted, no deduction, planned length left stale on screen. Found while investigating why a real ~2.4 minute test call showed 15 minutes and deducted nothing.",
      "status": "backlog — deferred, discuss later",
      "comment": "Pre-existing bug, not caused by anything built recently (confirmed via git history — predates the current billing system entirely, just never got connected to it). Confirmed Arun was undercharged on a real test call (2.4 real minutes, 0 deducted). Fix identified: route this path through the same 'properly close out a call' function everything else already uses (already safe to call twice). Deferred by Arun 2026-07-07 — 'we will come to this billing question later.' Also logged in BACKLOG.md (RECALL-WEBHOOK-BILLING-GAP). Not built. Whether to correct Arun's balance for the undercharged call is also still open."
    },
    {
      "id": "duration-fallback-logic-fix",
      "category": "Content & session lifecycle",
      "item": "Session length: fix fallback logic going forward (no historical repair)",
      "description": "Some places in the code checked a session's length without preferring the user's originally chosen plan length, and content generation silently assumed 30 minutes when that value was missing instead of using the user's real preference.",
      "status": "fixed, deployed — ready to test on any new session",
      "comment": "Went through full CEO -> BA -> approval -> build. Fixed 4 spots: the single-session lookup, the pre-call balance check, the scheduling lookup, and content generation's silent 30-min fallback (now uses the user's real chosen preference first, 30 only as an absolute last resort). Confirmed the core logic that decides how many sessions a topic needs was untouched and was already correct. No historical session data touched or repaired, by explicit design. Live in production."
    },
    {
      "id": "overview-screen-content-and-readiness-guard",
      "category": "Content & how Clio speaks",
      "item": "Overview screen skipped instantly + content silently marked ready when it wasn't",
      "description": "The Overview screen had no real prepared content and no instruction telling Clio to slow down there, so she moved on almost instantly. Separately, one specific new topic's content was never actually generated, but the system still reported it as ready, so Clio taught from her own general knowledge instead of the real prepared material.",
      "status": "fixed, deployed — ready to test",
      "comment": "Went through full CEO -> BA -> approval -> build. Root cause for the false-ready bug: there were two separate places that could mark a session's content ready, and only one of them actually checked its work — the other could save 'ready' before its own double-check ran, and never corrected it if that check later failed. Fixed by making one single shared checkpoint that all of them must pass before ever saying ready. Also gave the Overview and Summary screens real content (built from the session's own real title/agenda/topics, not another AI pass) and told Clio to treat the Overview with the same care as any other topic. No historical/already-broken session repaired, by design — going forward only. Live in production — not yet observed on a real live call."
    },
    {
      "id": "realtime-visual-test-toggle",
      "category": "Testing tools",
      "item": "Test toggle: real-time visualization vs pre-built KB content",
      "description": "Arun wants to A/B test the original real-time visualization generator (built before the KB/pre-built-content pipeline existed) against the current approach, during a real live Hume-native call. A simple on/off toggle for this.",
      "status": "deployed (off by default) — Arun hasn't chosen to flip it on yet",
      "comment": "Went through full CEO -> BA spec -> approval -> build process per Arun's explicit instruction (no shortcuts, even for small/test tooling). Off by default = zero behavior change anywhere, verified via direct code search (not assumption) — confirmed touches nothing outside this one toggle and its two call sites. On: the live call skips pre-built content and generates the visualization live instead. Explicitly disposable/test-only, not a permanent feature. Code is live in production but the env var is not set — needs NEXT_PUBLIC_REALTIME_VISUAL_TEST=true set in Vercel before Arun can actually test it."
    }
  ]
}
